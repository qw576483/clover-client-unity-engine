# 引擎修复记录（E 编号）

> **本文件的位置是刻意的**：引擎改动影响**所有**项目，所以记录必须放在**引擎仓库**里。
> 放在某一个业务 demo 的目录下 = 下个项目看不见 ⇒ 同一个坑会被反复踩。
> （旧版 skill 写的是"落到 `<项目根>/tools/ai-skill/constraints.md`"，**那是个错误**，已订正。）
>
> 流程见 [`clover-ai-skill/patterns/engine-fix.md`](https://github.com/qw576483/clover-tools/blob/main/ai-skill/patterns/engine-fix.md)：
> **先最小复现 → 最小修复（不动公开签名）→ 修复前失败/修复后通过 → 记 E 编号 → 更新文档/注释/skill**。

---

## E1 · `Timer` 在 `timeScale = 0` 时永不触发（★ 影响所有"画面冻结但仍要走时间"的写法）

**日期**：2026-09-16　**文件**：`Runtime/Core/Timer.cs`

| # | 改动 | 说明 |
|---|---|---|
| E1 | `TimerEntry` 新增 `Unscaled` 标志 | 标记该条按真实时间推进 |
| E1 | `Tick(dt)` 改为 `e.Elapsed += e.Unscaled ? Time.unscaledDeltaTime : dt` | 关键一行；`dt` 来自 `Time.deltaTime`，`timeScale=0` 时恒为 0 |
| E1 | `ITimer` 新增 `AfterUnscaled(float, Action)` / `EveryUnscaled(float, Action)` | **纯新增**，未改动任何现有签名；`After` / `Every` 行为完全不变 |

### 缺陷本质

引擎的 `Timer` 是靠每帧传进来的 `dt` 累加推进的，而 `dt` 来自 `Time.deltaTime`。
**只要业务把 `Time.timeScale` 设成 0**（暂停菜单 / 结算屏 / GameOver —— 这些恰恰是最需要"几秒后自动做点什么"的场景），
`dt` 恒为 0 ⇒ **任何定时器都永远不再触发**，且**不报错、不打日志**，现象是"那张屏永久卡住"。

### 最小复现（修复前失败）

```
Time.timeScale = 0f;
Game.Timer.After(1f, () => Debug.Log("回调执行了"));
// 等 3 秒 → 【没有】任何输出
Time.timeScale = 1f;
// → 回调立刻补执行，自报 "unscaled 已过 62.05s"
```

> ⚠️ 复现必须在 **Play 模式**下做，且**探针第一行要先打 `Application.isPlaying`**。
> 本仓实测踩过：前两遍复现跑在编辑模式（`isPlaying=False`、`unscaledTime` 冻在 0.67），结论全废。

### 修复后自证（对照组）

`timeScale = 0` 下同时安排两个 1 秒定时器：
`AfterUnscaled` **执行** ✓、普通 `After` **不执行** ✓
—— 对照组同时成立，证明是"新增能力"，不是"悄悄改了旧行为"。

### 已知精度边界（必须知道）

`AfterUnscaled` 按 `Time.unscaledDeltaTime` 累加。实测：

| 场景 | 结果 |
|---|---|
| 编辑器**有焦点**（≈ 真实玩家） | `实测 4.00s`（对常量 4s，**准**） |
| 编辑器**失焦**（≈ AI 自动化） | 同一段代码测出过 `3.29s`（偏慢）与 `0.84s`（偏快）—— **不能当秒表用** |

⇒ **玩家体验不受影响**；但**AI 做自动化验证时不要拿它计时**，需要严格墙钟时间请自行用 `Time.realtimeSinceStartup`。

### 用法

```csharp
// 画面被冻结（timeScale = 0）但仍要延时 → 必须用这个
Game.Timer.AfterUnscaled(4f, () => Game.Fsm.Transition(FlowState.Menu));
```

### 首个消费方

`clover-project-super-mario` 的 `AppFlow.EnterGameOver`：原来用 `Timer.After(4f, …)` + `timeScale=0`
⇒ **GameOver 屏永久卡死**（实测卡 30 秒以上、零日志）。现已改用 `AfterUnscaled`。

---

## 同步的文档（E1 / E2）

按 [`clover-ai-skill/patterns/engine-fix.md`](https://github.com/qw576483/clover-tools/blob/main/ai-skill/patterns/engine-fix.md) §5 的"三处落点"，本次两个改动同步到：

| 落点 | E1 | E2 |
|---|---|---|
| 源码注释 | `Runtime/Core/Timer.cs`（`Unscaled` 标志与两个新方法的注释） | `Runtime/Presentation/UIWidgets.cs`（订正了原写的"业务不直接使用"） |
| 引擎仓库文档 | 本文件 | 本文件 |
| **站点文档 `clover-doc/`** | `client/development/event-timer-fsm.md`（Timer API 表 + 新增「暂停/结算界面的定时器永远不执行」）、`client/reference/api-cheatsheet.md`（Timer 表两行）、`client/reference/constraints.md`（**新增硬约束 G12**） | `client/development/ui-system.md`（新增「用代码搭 UI（`UIFactory`）」整节）、`client/reference/api-cheatsheet.md`（新增「UI 构件（UIFactory）」整节） |
| skill | `reference/engine-mental-model.md`（能力表补 `AfterUnscaled`） | `reference/engine-mental-model.md`（`UIFactory` 已公开） |

---

## E2 · UI 构件工厂 `UIFactory` 是 `internal` ⇒ **业务根本调不到**（能力没开口）

**日期**：2026-09-16　**文件**：`Runtime/Presentation/UIWidgets.cs`

| # | 改动 | 说明 |
|---|---|---|
| E2 | `internal static class UIFactory` → **`public static class UIFactory`** | 只改可见性，**方法签名一个没动**；`ToastLayer` / `FloatTextLayer` / `LoadingLayer` / `ConfirmLayer` / `RedDotRegistry` / `GuideLayer` **保持 internal**（业务从 `Game.UI` 的 Toast/Confirm/Loading 入口用） |
| E2 | 订正类注释 | 原文写着"**业务不直接使用**" —— 公开之后这句就是错的，必须改，否则文档与代码打架 |

### 缺陷本质

`UIFactory` 提供 `CreateNode / Stretch / CreateCentered / CreatePanel / CreateText / CreateButton / DefaultFont / UICamera`，
是"用代码搭 uGUI"的完整脚手架。但它和那一堆 Layer 一样是 **internal**，而
`InternalsVisibleTo` 只开了 `CloverEngine.Tests.*` 与 `CloverEngine.Editor`，**没有业务程序集**。

⇒ **业务想用代码搭 UI，只能自己再写一套锚点/铺满/文本的工具类** ——
这不是业务"爱造轮子"，是**引擎没开口**。
实测代价：一个业务项目自建了 7.7KB 的 `UIBuilder`，并因为"面板根节点没铺满"踩坑，
**而 `UIFactory.Stretch(rt)` 正好能防住它**。

### 最小复现（修复前失败）

业务侧写一行 `CloverEngine.UIFactory.CreateNode("x", parent);` 编译即失败：

```
error CS0122: 'UIFactory' is inaccessible due to its protection level
```

### 修复后自证

同一个引用文件留在工程里 → `recompile_status` = `failed:false, errors:[]`，
且 `SuperMario.dll` 与 `CloverEngine.Presentation.dll` 同一时刻重建 ✓
（判据用 `recompile_status` 这个**权威状态**，不要 grep 日志文件：
`clear_console` 清的是编辑器控制台窗口，**日志文件里历史错误行仍在**，很容易误判成"还是失败"。）

### 已知边界

- `UIFactory.DefaultFont()` 是**引擎内置字体**（LegacyRuntime/Arial/系统字体）；
  **像素风项目要自己的像素字体**（且字号必须按字体规格取整），那部分**仍由业务包装**，引擎不代劳。
- 通用件（Toast/飘字/Loading/确认框/引导）**仍走 `Game.UI`**，不要直接 new Layer。

### 首个消费方

`clover-project-super-mario` 的 `UI/UIBuilder`：机械部分（`Stretch`/`Panel`/`Node`/`TextButton`）
改为委托 `UIFactory`，**只保留项目特有部分**（NES 像素字体加载 + 字号对齐 16 的倍数 + SMB 配色常量）。

---

## E3 · `IResourceManager` 没有"同步取已驻留资源" ⇒ 业务为一行同步读只能绕开资源抽象

**日期**：2026-09-16　**文件**：`Runtime/Core/Contracts.cs`（接口）、`Runtime/Resource/ResourceManager.cs`（实现）

| # | 改动 | 说明 |
|---|---|---|
| E3 | `IResourceManager` 新增 `T TryGet<T>(string path)` | **纯新增**，未动任何现有签名 |
| E3 | 实现基于已有的 `_cache: path → Entry{Asset,RefCount}` | 只读，**不触发加载、不阻塞** |

### 缺陷本质

`IResourceManager` 只有异步加载：`LoadAsset<T>(path, cb)` / `LoadAsset<T>(path, progress, cb)` / `Release` /
`UnloadAll` / `Preload`。**没有任何"取已经装好的资源"的入口。**

于是业务只要需要**同步拿到**一个资源（进关时要立刻解析关卡文本、第一个面板出现前就要拿到像素字体），
就只剩一条路：**绕开 `Game.Res` 直接 `Resources.Load`** ✗
—— 那等于自己找存储：资源根前缀、缓存、LRU、卸载策略、热更后端**全部失效**，
换后端（AssetBundle / 远端）时这些点会静默地不跟着变。

**注意这是"能力缺口"不是"缺陷"**：项目当时能跑。所以不需要"修复前失败"式的运行复现 ——
但**业务被逼着绕开**这件事本身是可复现的（下面）。

### 最小复现（修复前失败）

业务侧写一行 `Game.Res.TryGet<Font>(path);` → 编译失败：

```
error CS1061: 'IResourceManager' does not contain a definition for 'TryGet' and no accessible extension method ...
```

（旁证：修之前，这类需求在业务里的实际形态就是 `Resources.Load<TextAsset>(...)` / `Resources.Load<Font>(...)`。）

### 修复后自证

① 同一个引用文件留在业务工程里 → `recompile_status` = `failed:false, errors:[]` ✓

② 运行时三态 + 一组对照（`Preload` 关卡文本）：

```
① 预加载前   TryGet = null                        ← 不触发加载（语义正确）
② 不存在路径 TryGet = null                        ← 不误报
③ 预加载后   TryGet = World1-1（26883 字符）       ← 同步取到真实内容
④ 对照组 异步 LoadAsset = World1-1                ← 旧接口行为未变（证明是"新增"不是"改了旧的"）
```

③ 业务回归：菜单截图 **25965 字节 = 与既有基准逐字节一致**（说明像素字体仍生效、没掉回默认字体）；
关卡日志 `关卡已解析 Levels/World1-1：瓦片 803` 与历史基准一致，且**全程零 Warn**（预热未迟到）。

### 语义与边界（必须知道）

- **纯读**：不影响引用计数、不改变 LRU 顺序 ⇒ 取到的对象**不归调用方持有**，**不要**对它 `Release`；
- **不触发加载**：没装进来就是 `null`。要"立刻拿到"的用法是**先 `Preload`，之后 `TryGet`**；
- 引擎**依旧不提供**阻塞式同步加载（那会卡主线程）—— 这是有意为之；
- 已销毁 / 已卸载的对象按"没有"处理（用 Unity 的 `== null`，不会把「假 null」交出去）；
- 类型不匹配（缓存里是别的类型）也返回 `null`。

### 用法 + 首个消费方

```csharp
// 启动期预热（异步装进来）
Game.Res.Preload(new List<string> { ResPaths.PixelFont, ResPaths.Level11 }, onDone: () => { /* 可以开面板了 */ });

// 之后同步取（不阻塞、不加载）
var font = Game.Res.TryGet<Font>(ResPaths.PixelFont);
var text = Game.Res.TryGet<TextAsset>(ResPaths.Level11);
```

`clover-project-super-mario`：`AppFlow.EnterBoot` 预热「像素字体 + 关卡文本」→
`UI/UIBuilder.Font`、`Flow/StageSession.LoadLevelText` 改用它同步取；
**业务侧 `Resources.Load` 的实际调用清零**（只剩注释里的说明）。

---

## E4–E16 · 2026-09-18 本轮客户端修复（★ 影响所有项目）

**日期**：2026-09-18　**范围**：`Runtime/**`（另含 `Samples~/LoginFlow`、`Tests/`）

> **证据口径（本轮统一，务必照此理解）**：本仓**未跑 Unity 实机**（无编辑器会话），
> 因此**没有实机截图**。以下证据只有两类：
> ① **离线编译 + 离线断言**：`Tools~/core-assert/` 链接真实源码跑断言（命令与结果见本节末），
>    覆盖 `Logger` / `Setting` / `Json` / `Event`；
> ② **离线静态核对**：给出 `文件:行`，以及"修复后注释逐条对上代码"的核对点。
> **凡未实机验证的项，都按此口径标注，不要读成"已在设备/编辑器上验证通过"。**

| # | 文件 | 改动 |
|---|---|---|
| E4 | `Runtime/Core/Logger.cs` | 日志落盘与跨天切文件的线程归属修正 + WebGL 降级 |
| E5 | `Runtime/Core/Setting.cs` | 写盘改原子替换（`.tmp` + `File.Replace`）+ 任何失败退化为内存存储 + WebGL 不落盘 |
| E6 | `Runtime/Core/Json.cs` | `Dump` 加循环引用检测 + `double` 类型保型（`1.0` 不再退化成 `1`） |
| E7 | `Runtime/Core/Event.cs` | 新增通配订阅 `*` / `**`（精确订阅先执行；退订语义与精确订阅一致） |
| E8 | `Runtime/Core/EntityPool.cs`（`IObjectPool`）、`Runtime/Presentation/ObjectPool.cs` | 对象池新增**空闲过期回收**（`IdleExpirySeconds` / `TrimIdle`），默认 0 = 关闭 |
| E9 | `Runtime/Core/EntityPool.cs`（`ReferencePool`） | 新增引用池：纯托管对象按类型复用（`Acquire<T>` / `Release<T>` / `Count<T>` / `Clear<T>` / `ClearAll`） |
| E10 | `Runtime/Network/Connection.cs`、`WebSocketConnection.cs`、`Network/Quic/QuicConnection.cs` | 帧/消息上限三条线路 + 服务端**统一 10 MiB**（软上限告警、硬上限断线） |
| E11 | `Runtime/Core/Game.cs` | `GameConfig.UseTls` 默认改为 **true**（不再默认明文） |
| E12 | `Runtime/Core/Contracts.cs`（`IWorldSync` / `ICloverScene` / `IMapData`）、`Presentation/*` | 实体号 / `scene_id` 全部对齐 **`ulong`**（与服务端 `uint64` 一致，不再用 `long`） |
| E13 | `Runtime/Network/NetworkManager.cs`、`Network/EngineLoginFlow.cs` | `session_key` **不再当恢复凭证**：`SetupSession` 传非空会话凭证会 Warn；恢复凭证只认 `PushPlayerFullSync.session_token` |
| E14 | `Runtime/Presentation/CloverPresentation.cs`、`Presentation/Entity.cs` | `EntityViewFactory` **接线**：`CloverPresentation.EntityView` 随 `Game.Launch` 挂载，实体销毁时同步释放工厂侧记录 |
| E15 | `Runtime/Presentation/UI.cs`（`SafeAreaFitter`）、`Presentation/UIWidgets.cs` | 新增安全区适配（刘海 / 挖孔 / 圆角屏），内置层（Toast / 浮字 / Loading / 确认 / 引导）自动挂 |
| E16 | `Runtime/Network/Transport.cs` | 删除无消费点的 `ITransportConnection.GetAddr()`（及其各实现与转发） |
| E-net-01 ~ 03 | `Runtime/Network/NetworkManager.cs` | Call 排队**总期限**（60s，`MinPendingHardTimeoutSeconds`）/ 组帧异常时清理配对项与定时器 / `EMsg.QueuePosition` 非法 JSON 不穿出 `Game.Tick`（**编号沿用代码注释里自标的 `E-net-0x`**，详见下节） |

### 缺陷本质（逐条）

- **E4**：`Logger` 只在"日期变化"时唤醒写线程，且日期由**调用方**先改写 ⇒ ① 同一天的日志全部积压
  在队列里不落盘；② 写线程据此判定"日期没变、无需切文件"，跨天**永不切**，还会拼出 `.log`
  这种无名文件（`Logger.cs:369` 的注释就是这段历史）。修复把日期归属交给写线程自己
  （`Logger.cs:294`），并**每条日志都唤醒**（`Logger.cs:261`）。WebGL 下既无线程也无本地 FS，
  构造不做任何 IO（`Logger.cs:164-166`）。
- **E5**：`File.WriteAllText` 直写目标文件是"边写边截断" ⇒ 写一半崩溃/磁盘满会**破坏既有配置**
  （玩家设置全丢）；目录非法时还会把异常抛回 `Game.Launch` **打崩启动**。修复为
  `settings.json.tmp` → `File.Replace` 原子替换（`Setting.cs:255-258`），任何写盘不可用
  （WebGL / 目录非法 / 无权限）都**退化为内存存储并留 Warn**，不抛异常（`Setting.cs:111-144`）。
- **E6**：`Dump` 遇到自引用对象会**无限递归**（进程直接 StackOverflow，连日志都没有）；
  且整数值的 `double`（`1.0`）被输出成 `1`，Parse 回来变成 `long` ⇒ 类型信息静默丢失。
- **E7**：此前只能订阅"确切事件名"，业务想监听一整类事件（`Net.**`）只能自己维护分发器。
  通配订阅由引擎提供，且**精确订阅先于通配执行**（避免"泛订阅"抢在具体处理之前）。
- **E8**：对象池原先只按 `MaxCapacity` 裁剪 ⇒ 峰值那一批实例会**一直占着内存**（只多不少）。
  新增按闲置时长回收（回收与容量裁剪同路径、惰性执行、默认关闭保持既有行为）。
- **E9**：每帧产生大量短命托管对象（输入帧 / 事件参数 / 临时 List）会把 GC 顶起来；
  `ReferencePool` 是**纯托管对象**的复用池，与 `IObjectPool`（GameObject）互补。
- **E10**：三条线路各自一份帧上限且与服务端不一致 ⇒ 大帧在客户端被当"合法"、到服务端被拒，
  或反过来在客户端就断线，两侧口径不同会出现**只在极端帧长下才复现**的断连。
- **E11**：`UseTls` 默认 false 等于"默认明文"，局域网/公网部署极易漏配（移动端还必须用 TLS）。
- **E12**：服务端对象号 / 场景 id 是 `uint64`，客户端若用 `long` 承接，高位为 1 的 id 会变成负数，
  表现为"实体找不到 / 场景映射不到"，且**不报错**。
- **E13**：`session_key` 是**会话通道加密密钥**（AES-256），把它当恢复凭证传进去 = 把密钥当令牌用：
  服务端按"恢复凭证"校验必然失败，而客户端会以为自己已具备恢复能力（重连静默退化成重登/卡住）。
- **E14**：`IEntityViewFactory`（异步加载模型 + 占位 + 竞态 + 归一化贴地）此前**没有接线**，
  业务即使知道它存在也拿不到实例 ⇒ 只能自己手写（并踩竞态 / 尺寸 / 贴地三个坑）。
- **E15**：`Screen.safeArea` 全仓此前无人读 ⇒ 刘海屏 / 挖孔屏上顶部 HUD 与底部按钮被遮挡。
- **E16**：`GetAddr()` 除各实现自身与 `SimulatedTransport` 的转发外无消费点（`Transport.cs:25` 注释），
  属"接口上挂着但没人用"的死条目。

### 自证方式

**① 离线编译 + 离线断言（真跑，覆盖 E4 / E5 / E6 / E7）**

宿主：`Tools~/core-assert/`（**链接真实源码**，只补
`UnityEngine.Debug` 与 `CloverEngine.Game.Logger` 两个替身；见其 `README.md`）。

```
$ cd Tools~/core-assert
$ dotnet run                                # 常驻平台
... ALL PASS          (退出码 0)
$ dotnet run -p:DefineConstants=UNITY_WEBGL  # WebGL 分支（常驻平台下是死代码，必须单独跑）
... ALL PASS          (退出码 0)
```

断言与 E 号的对应：

| E | 断言（实际输出） |
|---|---|
| E6 | `循环引用抛异常（不是栈溢出）-> FormatException`；`Dump(1.0d) 输出带小数点 -> 1.0`；`Parse(Dump(1.0d)) 仍是 double`；`long 极值往返保型`；`超深嵌套 parse 抛异常` |
| E7 | `精确订阅先执行、通配随后 -> exact,Net.*,Net.**`；``Net.* `` 只命中一段（不命中 `Net.A.B`）`；`Off("Net.*") 注销干净`；`带参通配收到载荷 -> 7` |
| E5 | `原子写: 临时文件已被替换（无 .tmp 残留）`；`往返: 新实例读回同一值`；`损坏配置: 不抛异常且回退默认值` + `已留档 .corrupt`；`非法目录: 不抛异常且内存内可读` |
| E5（WebGL） | `WebGL: Save 后不创建目录` / `不产生文件` / `内存内可读回` |
| E4 | `同一天日志落盘（不靠日期变化唤醒）-> 2026-09-18.log`；`落盘内容完整（3 条都在）`；`跨天切出新文件 2001-01-01.log`；`写线程自行取日期 → 下一条落回当天文件`；`无 .log 无名文件（旧缺陷形态）` |
| E4（WebGL） | `WebGL: 不创建日志目录` / `不落盘任何文件` |

**② 离线静态核对（E8–E16，未实机）** —— 每条的核对点（改后注释 ↔ 代码）：

| E | 核对点 |
|---|---|
| E8 | `Runtime/Core/EntityPool.cs:147-163`（契约：`IdleExpirySeconds` 语义、负数按 0、主线程惰性回收）↔ `Runtime/Presentation/ObjectPool.cs:17-24`（读写钳位）、`:47-71`（`TrimIdle` 按 `unscaledTime` 判超时）、`:123,188-190`（Spawn/Despawn 两处顺带回收） |
| E9 | `Runtime/Core/EntityPool.cs:286-404`（`IReferencePoolable` + `ReferencePool`：按类型分桶、`OnAcquire`/`OnRelease` 回调点、重复归还告警、`Count`/`Clear`/`ClearAll`）；离线用例已存在：`Tests/Editor/ReferencePoolTests.cs`（需 Unity Test Runner，本轮未跑） |
| E10 | `Runtime/Network/Connection.cs:105-109,185-195`（软/硬上限均 `10 << 20`，注释明写"三条线路 + 服务端统一"）、`:485-490`（硬超断线 / 软超告警）↔ `WebSocketConnection.cs:30-36` ↔ `Quic/QuicConnection.cs:46-50`（`MaxFrameSize = 10 << 20`，对应服务端 `maxQUICFrameSize`） |
| E11 | `Runtime/Core/Game.cs:47` `public bool UseTls = true;` ↔ `Runtime/Network/NetworkManager.cs:457`（`_useTls = cfg != null && cfg.UseTls` 透传）↔ `Transport.cs:298-302`（移动端建议直接启用 TLS 的成因） |
| E12 | `Runtime/Core/Contracts.cs:495,500,535-571`（`TryGetPosition(ulong)`、`OnEntity*/OffEntity*(ulong …)`，注释写明"服务端对象号为 uint64，故用 ulong"）↔ `ICloverScene.SceneID/RegisterMapping/ResolveUnityScene` 同改 `ulong`；⚠️ **残留不一致（未改，见下方"已知残留"）**：`IEntityManager` / `EntityInfo.ObjectID` / `IEntityViewFactory` 仍是 `long` |
| E13 | `Runtime/Core/Contracts.cs:304-307`（`SetupSession` 的恢复凭证参数**应传空**）↔ `NetworkManager.cs:702-718`（收到非空凭证打 Warn 指出误用）↔ `EngineLoginFlow.cs:268-270`（登录后传空，等 `PushPlayerFullSync.session_token` 覆盖） |
| E14 | `Runtime/Presentation/CloverPresentation.cs:45-57,114-115,131`（`EntityView` 属性 + 构造即注册 + 挂载日志列出 `Entity(+EntityView)`）↔ `Presentation/Entity.cs:13-19`（注册槽）、`:176-266`（`EntityViewFactory` 实现）、`:148`（实体销毁时一并释放工厂侧记录） |
| E15 | `Runtime/Presentation/UI.cs:469-508`（`SafeAreaFitter`：`Screen.safeArea` 变化才重算，非 `RectTransform` 节点打 Warn 停用）↔ `Presentation/UIWidgets.cs:212,338,450-451,606-607`（内置层 / 面板挂载点） |
| E16 | `Runtime/Network/Transport.cs:25`（删除说明注释）；全仓 `grep GetAddr` 只剩 `LanBrowser` 里的 `GetAddressBytes()`（IPAddress 的方法，非本接口） |

**③ 本轮同时新增/更新的离线用例（需 Unity Test Runner，本轮未跑）**：
`Tests/Editor/EventBusWildcardTests.cs`（E7）、`Tests/Editor/ReferencePoolTests.cs`（E9）、
`Tests/Editor/ResourceManifestIndexTests.cs`。

### 已知残留（本轮**未**消除，别当已修）

1. **E12 只做了一半**：网络与场景侧已全部 `ulong`，但 `IEntityManager` / `EntityInfo.ObjectID` /
   `IEntityViewFactory` 仍是 `long` ⇒ 业务把 `Game.Sync.OnEntityEnter` 给的 `ulong` 传给
   `Game.Entity.BindView(long, GameObject)` **必须显式转换**。统一它属**公开签名变更**，
   需授权后再做（本轮不动公开签名）。
2. **`Tools~/quic-harness` 之外的另一个离线宿主 `client-link-probe` 当前编译不过**
   （`.codebuddy/doc-audit/tools/client-link-probe/`，27 处 CS 错误：缺 `SessionCrypto` /
   Quic 源码 / `CloverEvents` 等链接项）—— 属既有失效工具，本轮未改；
   本轮新增的 `Tools~/core-assert/` 不依赖它。

---

## E-net-01 ~ E-net-03 · 2026-09-18 本轮客户端修复（`NetworkManager` 三条，续 E4–E16）

**日期**：2026-09-18　**文件**：`Runtime/Network/NetworkManager.cs`

> **编号说明**：这三条在本轮改代码时就已**在源码注释里自标编号**（`NetworkManager.cs:91` `E-net-01`、
> `:892` `E-net-02`、`:1010` `E-net-03`），故沿用 `E-net-0x`，不占用主序列 `E17+`。
> 与本文件既有的 `E1`~`E16`、`E-res-01` **均不冲突**（此前全文无 `E-net-` 前缀）。

| # | 文件 | 改动 |
|---|---|---|
| E-net-01 | `Runtime/Network/NetworkManager.cs` | 单个 Call 新增**总期限**（`MinPendingHardTimeoutSeconds = 60`）：排队位置帧只顺延到 `HardDeadlineTicks` 为止 |
| E-net-02 | `Runtime/Network/NetworkManager.cs` | `Call` 的序列化 / 组帧 / 发送整段包进 `try`：异常时立即失败请求并清理配对项与定时器，不让 `ClientFrame.Encode` 的异常逃逸 |
| E-net-03 | `Runtime/Network/NetworkManager.cs` | `EMsg.QueuePosition` 反序列化包 `try/catch`：非法 JSON 只记 Error，不再穿出 `DrainFrame` → `Game.Tick` |

### 缺陷本质（逐条）

- **E-net-01**：排队期间每收到一条位置帧就顺延一次未决请求的超时（`ExtendPendingDeadlines`，
  `NetworkManager.cs:400-428`）——**只有顺延、没有上限**。服务端只要持续刷新排队位置，在途 Call
  就永远不会超时，业务永远等不到失败结论；而排队停滞期服务端**不下发位置帧**，靠顺延语义本身也兜不住。
  修复：`PendingCall.HardDeadlineTicks`（`:232-236`）在发起时按
  `max(_callTimeoutSeconds, MinPendingHardTimeoutSeconds)` 定死（`:863-865`），顺延只到该时刻为止
  （`:409-416` 超限即 Warn 并停止顺延）。
- **E-net-02**：`Call` 原先在建立配对项与超时定时器**之后**才做序列化 / 组帧 / 发送，而
  `ClientFrame.Encode` 在帧体超限时抛 `ArgumentException` ⇒ 异常直接逃逸出调用方，
  而配对项与超时定时器都已建好：请求只能干等超时，字典项也残留到超时为止
  （`:890-892` 注释即为此）。修复：整段包进 `try`，catch 内 `FailPending(...)` 立即以异常结束该请求
  并清掉配对项与定时器（`:916-921`；`FailPending` 见 `:932-941`，`TryRemove` + `ReferenceEquals` 保证竞争安全）。
- **E-net-03**：`EMsg.QueuePosition` 的 body 用 `Serializer.Deserialize<EQueuePositionNotify>` 解析，
  底层 `JsonUtility` 对非法 JSON 是**抛异常**而非返回 null ⇒ 异常穿出 `DrainFrame` → `Game.Tick`，
  主循环每帧抛一次（`:1009-1010` 注释即为此）。修复：解析包 `try/catch`，失败记 Error 后按
  「无有效位置帧」继续（`:1014-1021`，`pos == null` 直接 return）。

### 自证方式（离线静态核对，同 E4–E16 口径：本轮**未跑** Unity 实机）

| # | 核对点（改后注释 ↔ 代码） |
|---|---|
| E-net-01 | `NetworkManager.cs:89-99`（常量语义 + 为什么必须有总期限）↔ `:232-236`（`HardDeadlineTicks`）↔ `:863-865`（发起时定死）↔ `:400-428`（顺延封顶 + 超限 Warn） |
| E-net-02 | `NetworkManager.cs:890-892`（为什么必须在 `try` 内）↔ `:893-921`（`try` + catch→`FailPending`）↔ `:932-941`（竞争安全的失败收尾） |
| E-net-03 | `NetworkManager.cs:1009-1010`（为什么不包 catch 会每帧抛）↔ `:1011-1024`（`try/catch` + `pos == null` return） |

> 注：`NetworkManager.cs:895` 注释里的「body > 1MB」曾是 `ClientFrame.Encode` 上限的**陈旧文字**
> —— 同批 `E10` 已把三条线路帧上限统一为 **10 MiB**（现 `ClientFrame.MaxBodySize = 10 << 20`，
> `Runtime/Network/Connection.cs:111`）。**本轮已把该行注释改成 10 MiB**
> （原注记「该行注释**未改代码**」随本轮作废）；对外契约同步：`结构规则.md` §五 `N2` 已写明
> 「单帧上限三条线路统一 10 MiB」。详见下节「本轮客户端修复（续 …）」的开头说明。

---

## E-editor-01 · MapBake 取样柱按**绝对 Y** 解释 ⇒ 地面 Y≠0 的关卡烘出「全可走」空地图（无告警）

**日期**：2026-09-18　**文件**：`Editor/MapBake/MapBakeOptions.cs`、`Editor/MapBake/MapBaker.cs`、`Editor/MapBake/MapBakeWindow.cs`

> **编号说明**：本条代码里**未自标编号**（三个文件均无 `E-` 标记），按本文件既有的
> 「`E-<域>-<序号>`」体例（同 `E-res-01` / `E-net-01`）取 **`E-editor-01`**（MapBake 属 Editor 横切），
> 不占用主序列 `E17`；与既有编号无冲突。

| # | 文件 | 改动 |
|---|---|---|
| E-editor-01 | `Editor/MapBake/MapBakeOptions.cs` | `ProbeBottomY` / `ProbeTopY` 语义由**绝对 Y** 改为**相对地面顶面 `GroundTopY`**；`Validate` 增「`ProbeBottomY` 须 > 0」 |
| E-editor-01 | `Editor/MapBake/MapBaker.cs` | 取样柱实际 Y = `GroundTopY + Probe*`；出生点回退位置以 `GroundTopY` 定高，并把「格」按 `CellSize` 换算成米 |
| E-editor-01 | `Editor/MapBake/MapBakeWindow.cs` | 「按包围盒推算」与烘焙的失败都显**错误态**；面板 / 菜单改走**帧驱动**导出（可取消进度条） |

### 缺陷本质

- **取样柱语义**：`ProbeBottomY` / `ProbeTopY` 旧实现按**绝对 Y** 摆放取样柱（默认 0.2~2.2）。
  地面 Y≠0 的关卡（例如地面在 10m）取样柱整根埋在地下，**逐格都不与障碍相交** ⇒ 烘出
  「全可走」空碰撞地图，**且没有任何告警**；玩家进图后本地不阻挡而服务端阻挡 = 橡皮带。
  修复：取样柱以地面顶面为基准（实际世界 Y = `GroundTopY + Probe*`，`MapBaker.cs:327-333`），
  默认值在任何地面高度都可用；并把「底面须严格高于地面」写成校验
  （`MapBakeOptions.cs:93-99`：`ProbeBottomY <= 0` 直接判非法）。
- **出生点回退**：场景里没有出生点标记时回退到「地图两角内缩 4m」——旧实现漏乘 `CellSize`
  （地图尺寸单位是**格**），`CellSize ≠ 1` 时第二个角落到地图中间或图外，玩家一出生就被本地碰撞锁死。
  修复：以 `GroundTopY` 定高、按 `CellSize` 换算成米（`MapBaker.cs:459-465`）。
- **面板失败态 / 帧驱动**：①「按场景包围盒推算」旧实现无条件按成功样式显示，用户会把
  「场景不存在 / 没有几何」当成推算成功；现由 `TryFitBoundsToScene` 回传成败，失败走**错误态**
  （`MapBakeWindow.cs:70-83`、`MapBaker.cs:489`）。② 面板与菜单旧实现**同步**跑 `Export`，
  大图（`MaxDimension = 32768`）下点一次把编辑器冻住数分钟且无反馈；现改走
  `MapBaker.ExportInteractive`（逐格切到每帧一块 + **可取消**进度条，结果由回调写回面板）
  （`MapBakeWindow.cs:162-191`、`MapBaker.cs:104-115`）。CLI 入口 `Export` 仍是同步跑（无人值守无帧循环）。

### 自证方式（离线静态核对，同 E4–E16 口径：本轮**未跑** Unity 实机）

| # | 核对点（改后注释 ↔ 代码） |
|---|---|
| E-editor-01 | `MapBakeOptions.cs:50-62`（字段语义：相对 `GroundTopY`）↔ `:93-99`（`ProbeTopY > ProbeBottomY` + `ProbeBottomY > 0`）↔ `MapBaker.cs:297-334`（`GroundTopY + Probe*` 求取样柱范围）↔ `MapBaker.cs:446,459-465`（出生点定高 + 格→米换算）↔ `MapBakeWindow.cs:70-83,91-93,162-191`（失败态 + 帧驱动 + 面板文案「相对地面高度」） |

---

## E-core-01 ~ E-core-03 / E-input-01 / E-res-02 / E-audio-01 · 2026-09-18 本轮客户端修复（续 E4–E16 / E-net-01~03 / E-editor-01）

**日期**：2026-09-18　**文件**：`Runtime/Core/Game.cs`、`Runtime/Core/Json.cs`、`Runtime/Core/Input.cs`、
`Runtime/Presentation/Input.cs`（另新增 `Tests/Editor/InputManagerTests.cs`）、
`Runtime/Resource/ResourceDownloader.cs`、`Runtime/Presentation/Sound.cs`、
`Runtime/Network/NetworkManager.cs`（仅注释一行）

> **证据口径（同 E4–E16，务必照此理解）**：本仓**未跑 Unity 实机**（无编辑器会话），因此**没有实机截图**。
> 本节证据只有一类 —— **离线静态核对**：给出 `文件:行` 与「改后注释 ↔ 代码」的核对点。
> `Tests/Editor/InputManagerTests.cs` 的 10 条是 **EditMode 用例，本轮未跑**（需 Unity Test Runner）。
> **凡未实机验证的项都按此口径标注，不要读成"已在设备/编辑器上验证通过"。**

> **编号说明**：`E-core-` / `E-input-` / `E-audio-` 三个前缀**此前全文未用过**；`E-res-` 只被 `E-res-01` 占用，
> 故续 **`E-res-02`**。六条均不占用主序列 `E17+`，与既有 `E1`~`E16`、`E-net-01~03`、`E-editor-01`、`E-res-01`
> **均不冲突**（编号来源同 `E-editor-01`：代码里未自标编号，按本文件既有「`E-<域>-<序号>`」体例取）。
> 其中 **`E-core-03` 与 `E-input-01` 是同一改动的两半**（契约层 / 实现层），故意分开记：
> 只改契约的人与只改实现的人都能各自找到出处。

> **同轮另订正两处文档/注释漂移（不单开编号 —— 它们是对既有条目的订正，不是新缺陷）**：
> ① `Runtime/Network/NetworkManager.cs:895` 的注释原写 `body > 1MB`，而现常量是
> `ClientFrame.MaxBodySize = 10 << 20`（`Runtime/Network/Connection.cs:111`）⇒ **已把该行注释改成 10 MiB**
> （下方 `E-net-02` 段落那条「该行注释**未改代码**」的旧注记随之作废，已就地改写）；
> ② `结构规则.md` §五 `N2` 已写明「**单帧上限三条线路统一 10 MiB**」（与 `E10` 落地的常量一致）。

| # | 文件 | 改动 |
|---|---|---|
| E-core-01 | `Runtime/Core/Game.cs`、`Runtime/Network/NetworkManager.cs` | 网络参数默认值**收敛到唯一出处**：`GameConfig` 新增 6 个 `public const Default*`（心跳 15000ms / 连接超时 5000ms / Call 超时 10s / 重连 5 次 / UDP 保活 10s / ws 路径 `/ws`），`NetworkManager` 构造兜底改为引用它们（不再各抄一份字面量） |
| E-core-02 | `Runtime/Core/Json.cs` | `MiniJson.ParseNumber` 整数超 `long` 时先试 `ulong`、再不行**按原文保留为 `string` 并 Warn**，不再静默退化为 `double`（雪花 ObjectID 曾精度丢失） |
| E-core-03 | `Runtime/Core/Input.cs` | `InputState` / `IInputManager.State` 补**契约注释**：`State` 交出去的是**副本**，业务写它不得篡改引擎本帧输入 |
| E-input-01 | `Runtime/Presentation/Input.cs`、`Tests/Editor/InputManagerTests.cs` | `InputManager.State` 改为把内部快照拷进**一块复用的缓冲**再返回（不每帧 new）；新增 10 条 EditMode 用例守住「后端选择 / 锁语义 / 副本语义」 |
| E-res-02 | `Runtime/Resource/ResourceDownloader.cs` | 新增 `#if UNITY_WEBGL` 分支：**只下到内存**（内存 MD5 校验、不建目录不落盘 + 一条 Warn 说明不持久化）；非 WebGL 路径逐行走原逻辑 |
| E-audio-01 | `Runtime/Presentation/Sound.cs` | 订阅 `CloverEvents.App.Pause` / `App.Resume`（暂停记账 / 恢复 / `Dispose` 退订）—— 这两个事件此前**发布了但全仓无订阅者**（死事件） |

### 缺陷本质（逐条）

- **E-core-01**：同一组网络默认值**在两处各写一份** —— `GameConfig` 的字段初始化器一处、
  `NetworkManager` 构造兜底一处（`:454-455` 的注释就是这段历史）。这种"双份默认值"的要害是**改一处不会报错**：
  把 `DefaultCallTimeoutSeconds` 调成 15 的人只改了初始化器，而把 `CallTimeoutSeconds` 留 0
  （或用了不完整的 `configOverride`）的调用方仍拿到旧值 10 ⇒ 同一进程内**两套默认值并存**，
  现象是"线上超时时间跟本地测出来的不一样"，且没有任何日志。修复：常量只定义一处
  （`Game.cs:19-35`，`:12-17` 注释写明"唯一出处"），字段初始化器（`:99,105,111,118,124` 与 `:60`）
  与 `NetworkManager` 兜底（`:456-461`）全部引用它。
- **E-core-02**：`ParseNumber` 原先对整数只有 `long.TryParse`，失败即 `double.Parse`。
  `double` 只有 **53 位有效尾数**（≈9.0e15），而服务端对象号是 **uint64**、雪花 ID 更是贴着 2^63 ——
  超过 2^53 的大整数会被**就近取整**，Parse 出来的值与原文**不是同一个数**，
  且**不抛异常、不打日志**（表现为"实体号 / 场景映射对不上，却查无异常"）。
  修复后的降级顺序是 `long` → `ulong`（uint64 放得下就精确保住，`:557`）→
  **原文 `string` + Warn**（`:560-562`）：宁可让调用方遇到"类型不是数字"（可见、可查），也不静默丢精度。
  类注释（`:12-15`）同步写明产物映射含 `ulong` 与"超 `ulong` 按原文保留 string"。
  **与 `E6` 不冲突**：E6 改的是 `Dump` 侧（循环引用 + `1.0` 保型），本条是 `Parse` 侧。
- **E-core-03**：`InputState` 的九个字段是 `public` 可写（历史签名，本轮按"不改公开签名"的纪律不动），
  而 `IInputManager.State` 的契约原文**没有"副本"这一层** ⇒ 实现把**引擎内部那个实例**交出去
  （`Presentation/Input.cs:664-665` 的注释即为这段历史）。业务一句 `Game.Input.State.JumpDown = true`
  就能改掉引擎本帧输入快照；这类写入**不报错、离线查不出**，
  表现为"偶尔凭空跳跃 / 位移"，业务还看不出是自己写的。
  修复：把只读语义写进契约（`:74-83`，并说明"字段仍 public 可写只是历史签名，真正的只读由这一层保证"），
  实现侧改为副本（见 E-input-01）。
- **E-input-01**：承接 E-core-03。实现侧把内部 `_state` 的九个字段拷进**一块复用的** `_snapshot` 再返回
  （`:678-693`）：既不再外泄内部实例，也不产生"每次读取 new 一个"的逐帧 GC ——
  而后者恰恰是当初直接交出内部实例的动机，所以修复必须用**复用缓冲**而不是每次 `new`。
  同时补 10 条 EditMode 用例（`Tests/Editor/InputManagerTests.cs`），守三件"实现了但容易被后人改坏"的事：
  ① 后端选择语义（`:35-71`）；② 锁语义 —— `Lock` 期间一切读取返回默认值、含 `MousePosition` / `HasTouch`（`:75-121`）；
  ③ 副本语义（`:125-150`）；另加"空后端逐读返回默认且不抛"（`:154-210`）。
  用例**刻意不测**真实按键 / 触摸：那依赖真机与编辑器焦点，断言它们会因为"跑测试的机器没有键鼠"而 flaky。
- **E-res-02**：`ResourceDownloader` 的整套可靠性都建在**文件系统**上：半成品 `*.part` + 校验通过才改名落地 +
  落地后按清单 hash 校验（类注释 `:129-137` 的"三道保险"）。而 WebGL 上 `System.IO` 落在 Emscripten 的
  **内存**虚拟 FS —— 页面刷新即丢，也拿不到 IndexedDB 同步 ⇒ 三道保险在该平台**整体失去意义**，
  还会给出一份"看起来下好了"的假状态（`CreateDirectory` 会成功、`File.Exists` 能探到本会话写过的 `.part`
  并据此算续传 offset、`_finishedFiles` 照样结账）。修复：本平台整条走内存路径 —— `Start` 不建内容目录
  （`:321-334`）、`BeginOne` 先打一条 Warn 说明"本平台不做持久化"再转 `BeginOneInMemory`（`:511-513`、
  `:611-641`：`DownloadHandlerBuffer` 收内容、`Stream = null`、不带 `Range`）、`FinishOne` 必须在释放请求
  **之前**把内容取出来（`:655-659`）、`CompleteFile` 转 `CompleteFileInMemory`（`:716-723`）用
  `ResourceHash.BytesMd5Hex`（`:90-109`，仅 WebGL 编译进来）在**内存内容**上做同一套 MD5 比对，
  且**校验通过才计入完成**（`:766-790`），与落盘路径共用同一个收尾门控（`_hashing` 计数，`Tick` 不会提前结账）。
  非 WebGL 路径一行不变（`BeginOne` 的 `#else` 分支 `:514-602`、`CompleteFile` 的 `#else` 分支 `:724-757` 逐行保留）。
  与 `E4` / `E5` 的"WebGL 不落盘"是同一口径（那两条分别在 Logger / Setting）。
- **E-audio-01**：`EngineRunner.OnApplicationPause`（`Runtime/Core/EngineRunner.cs:52-58`）一直在
  `Emit(CloverEvents.App.Pause / App.Resume)`，但**全仓没有任何订阅者** ⇒ 切后台时音频照响、
  回前台状态错乱，而事件本身"看起来在工作"（有 Emit 点、无报错），是最典型的一类死事件。
  修复：接上引擎内的最小消费者 —— `SoundManager` 构造订阅（`:77-83`；`Game.Event` 为 null 时静默跳过，
  供 EditMode 里直接 `new SoundManager()` 的情形）；暂停时用 `_appPausedSources` **自己记账**
  （`:39-41` 说明为什么不能靠 `AudioSource.isPlaying` 反推：暂停态它返回 false），
  恢复时按记账 `UnPause`（`:108-118`，对"假 null"跳过）；`Dispose` 先退订再拆（`:314-320`），
  否则 Shutdown 之后若事件总线还活着（或被重新 Emit），回调会落到已销毁的音源上。

### 自证方式（离线静态核对，同 E4–E16 口径：本轮**未跑** Unity 实机）

| # | 核对点（改后注释 ↔ 代码） |
|---|---|
| E-core-01 | `Runtime/Core/Game.cs:12-17`（"唯一出处"注释：为什么不能让第二处再写一份）↔ `:19-35`（6 个 `public const Default*`）↔ `:60,99,105,111,118,124`（字段初始化器全部引用常量）↔ `Runtime/Network/NetworkManager.cs:203-204`（"兜底值只取自 GameConfig 默认常量，不要在本文件再写字面量"）↔ `:454-462`（0 值 / null 兜底六项全引用 `GameConfig.Default*`）；**grep 佐证**：对 `Runtime/Network/NetworkManager.cs` grep `15000` / `5000` / `10000` 均**无命中**（该文件里唯一的 60s 是 `MinPendingHardTimeoutSeconds`，与这组默认值无关） |
| E-core-02 | `Runtime/Core/Json.cs:12-15`（产物映射：`long` / `ulong` / 超 `ulong` 保留 string）↔ `:540-563`（`ParseNumber`：`:550` long → `:557` ulong → `:560-562` 原文 string + `Game.Logger.Warn`）；E6 侧对照（**本轮未动**）：`:52-68`（`Dump` 的加固注释）↔ `:79-81`（深度上限）↔ `:176-203`（循环引用检测）↔ `:142-174`（浮点保型） |
| E-core-03 | `Runtime/Core/Input.cs:38-46`（`InputState` 注释："成员保留 public 读写，但只读语义由 `IInputManager.State` 那一层保证"）↔ `:74-83`（`State` 契约：返回副本 + 内部快照拷进**复用的**缓冲 + 同一帧多次读取是同一实例别跨帧存） |
| E-input-01 | `Runtime/Presentation/Input.cs:663-666`（`_state` 引擎内部 vs `_snapshot` 对外缓冲）↔ `:678-693`（九个字段逐个拷贝后返回）↔ `Tests/Editor/InputManagerTests.cs:35-210`（10 条 `[Test]`，其中 `:125-141` 断言"改副本不污染引擎"、`:143-150` 断言 `AreSame(mgr.State, mgr.State)`、`:75-106` 断言锁定下 `State` 全零且含 `MousePosition` / `HasTouch`） |
| E-res-02 | `Runtime/Resource/ResourceDownloader.cs:138-144`（类注释的 WebGL 例外）↔ `:241-249`（`_memoryFiles` 及原因）↔ `:321-334`（不建内容目录）↔ `:505-513`（Warn 文案 + 转内存路径）↔ `:605-642`（`BeginOneInMemory`）↔ `:655-659`（释放请求前取内存字节）↔ `:716-723` / `:760-791`（内存 MD5 校验，通过才计入完成）↔ `:90-109`（`BytesMd5Hex` 仅 WebGL 编译） |
| E-audio-01 | `Runtime/Presentation/Sound.cs:77-83`（构造订阅）↔ `:89-106`（暂停 + 记账）↔ `:108-118`（恢复）↔ `:314-320`（`Dispose` 先退订）↔ 发布方 `Runtime/Core/EngineRunner.cs:52-58`；**grep 佐证**：全仓 `rg "CloverEvents\.App\.(Pause\|Resume)"` 命中 2 个文件 —— `Sound.cs` 6 处（`On`/`Off` 4 处 + 注释 2 处）、`EngineRunner.cs` 2 处（`Emit`），**再无其它订阅者** |

> 本轮新增的离线用例（需 Unity Test Runner，**本轮未跑**）：`Tests/Editor/InputManagerTests.cs`（E-input-01 / E-core-03，
> 10 条 EditMode）；与 E4–E16 新增的 `EventBusWildcardTests` / `ReferencePoolTests` / `ResourceManifestIndexTests` 并列。

---

## E-res-01：`Resources.LoadAsync` 在同帧已被同步取过时 `completed` 不触发 ⇒ 异步加载永久卡死

**现象（业务侧表现）**：进图后角色/怪物的贴图**永远停在纯色占位**；日志里既没有"加载失败"也没有成功的记录；
换素材、改命名、改导入设置**全部无效**（因为根因不在业务侧）。

**根因**：`Runtime/Resource/ResourceBackend.cs` 的 `ResourcesBackend.BeginLoad` 只靠
`req.completed += …` 交付回调。若该资源在**同一帧内已先被同步 `Resources.Load` 取过**，
`Resources.LoadAsync` 会返回一个**已经 `isDone == true`** 的请求，此时 `completed` **不会再触发**
⇒ 上游 `ResourceManager` 的 pending 永远留在 `_inflight`，该路径此后一直加载不出来。

**最小修复**（`ResourceBackend.cs` `BeginLoad`）：订阅之前先判 `isDone`，立即内联回调：
```csharp
if (req.isDone) { onDone?.Invoke(req.asset); return req; }
req.completed += op => onDone?.Invoke((op as ResourceRequest)?.asset);
```

**最小复现**：`clover-project-diablo2/client/_dev/p_a11_probe2.cs`（先同步 `Resources.Load` 同一路径，再走异步链路），
原始输出 `.../client/_dev/a11_probe2.txt`。

**回归验证**：修复后进 Play，角色/怪物渲染从"纯色占位"变为 `.dcc` 解出的原版逐帧贴图。

## E-build-01 · 引擎 `Tests/Editor` 编译不过 ⇒ **下游工程全都进不了 Play**（2026-09-18）

**日期**：2026-09-18。**文件**：`Tests/Editor/InputManagerTests.cs`

| # | 改动 | 说明 |
|---|---|---|
| E-build-01 | `backend.GetAxis("Horizontal")` → `backend.GetAxis("Horizontal", false)` | `NullInputBackend.GetAxis`（`Runtime/Presentation/Input.cs:652`）的签名是 `GetAxis(string axis, bool raw)`，**没有默认值**；接口 `IInputBackend.GetAxis(string, bool)`（同文件 `:26`）也没有。只有 `Runtime/Core/Input.cs:140` 的 `IInputManager.GetAxis(string, bool raw = false)` 才有默认值。测试用**具体类型** `var backend = new NullInputBackend(...)`（`:167`）按单参形态调用 ⇒ **CS7036** |

### 缺陷本质

`NullInputBackend.GetAxis` 是两个必填参数，而测试按「有默认值」的写法调它 ⇒ 该测试程序集编译失败。

**为什么影响所有下游工程**：`client/Packages/manifest.json` 的 `testables` 含 `com.clover.unity-engine`
⇒ 引擎包里带错误的程序集被纳入编译目标；而 **Unity 只要有任何程序集编译失败就拒绝进 Play**
（实测：`editor_play` 回报 `Entered play mode`，但 `EditorApplication.isPlaying == False`、`editor_status.playMode == "stopped"`，
且 `Game.dll` 之类**已经**编译成功 —— 所以光看 `Diablo2.dll` 的时间戳会误判成"编译没问题"）。
⇒ 症状不是"某个测试红了"，而是**下游工程一个都验不了**。

### 最小复现（修复前失败 / 修复后通过）

```bash
cd <任一引用该引擎的下游工程>/client
unity command recompile && unity command recompile_status
# 修复前：{"status":"completed","failed":true,"errors":["...Tests/Editor/InputManagerTests.cs(182,41): error CS7036 ..."]}
# 修复后：{"status":"completed","failed":false,"errors":[],"compilationFailed":false}
unity command editor_play && unity command eval "return UnityEditor.EditorApplication.isPlaying;"
# 修复前：result=false（Play 起不来）        修复后：result=true
```

### 回归验证

某下游工程本轮实机取证（技能树面板）在修复后**连续两次进 Play** 全部通过。
改动只落在 `Tests/Editor` 的这一个调用，**引擎 `Runtime/**` 一行未动**，业务代码一行未动。

---

## E-build-02 · SFX 音源池硬编码 8 个 ⇒ 池满即静默丢弃（2026-09-19，**补登记**）

**日期**：2026-09-19。**文件**：`Runtime/Presentation/Sound.cs`

| # | 改动 | 说明 |
|---|---|---|
| E-build-02 | 新增 `private const int SfxPoolSize = 32;`（`:31`）并把它用在音源池构造循环（`:71` `for (var i = 0; i < SfxPoolSize; i++)`） | 原为硬编码 `for (var i = 0; i < 8; i++)` |

### 缺陷本质

音源池只有 8 个 `AudioSource`，池满时取源分支走"丢弃本次播放 + 告警一次" ⇒ **音效静默丢失**。一次命中会同时触发 挥砍 / 命中 / 受击 / 死亡 / 掉落 若干音效，AOE / 群体战下 8 个源不够。

### 自证 / 首个消费方

`clover-project-diablo2` 片 17 实机（探针扫场上 `AudioSource`）：`[P17] total AudioSource=34 ; SFX* count=32 (expect 32)` + `assert SFX pool == 32 ? True`（34 = 32 SFX + 2 BGM）；引擎包改后 `unity command recompile` = `up_to_date / failed=false / errors=[]`。多挂的 24 个源是**空载** `AudioSource`（各自独立子节点），无行为副作用、纯增益。

> ⚠️ 本条在改代码当时只登记到了业务项目的 `tools/ai-skill/constraints.md`（"本项目依赖的引擎修复"表），**引擎仓库漏登** ⇒ 该表里那句"出处：引擎仓库 `修复记录.md`"当时**查不到**（悬空，按 skill §1.11 第 5 条"引用路径可达"判为不合格）。2026-09-19 收尾轮**补齐本节目** ⇒ 该引用现已可达。

---

## E-build-03 · `SceneModule.Load` 的 AsyncOperation 泄漏 ⇒ 僵尸场景累积、后续 `LoadSceneAsync` 的 progress 恒 0（2026-09-19）

**日期**：2026-09-19。**文件**：`Runtime/Presentation/Scene.cs`（`SceneModule.Load`）

| # | 改动 | 说明 |
|---|---|---|
| E-build-03 | 新增字段 `private AsyncOperation _progressOp;` / `private string _progressScene;` | `_progressOp` = "最近一次 Load 的 op"，`_progressScene` 只给那条 Warn 用 |
| E-build-03 | 停旧轮询（`Game.Timer?.Stop(_progressTimerId)`）**之前**：若上一个 op 仍 `!isDone` ⇒ 先 `pending.allowSceneActivation = true` + 一条 `Game.Logger?.Warn("Scene", …)` | 旧加载**不再被等待**，但不许它烂在"永不激活"上 |
| E-build-03 | 轮询回调首行 `if (_progressOp != op) return;`；`completed` 里清字段前判 `if (_progressOp == op)` | `Timer.Stop` 延迟到 Tick 生效 ⇒ 被顶掉的旧 op 本轮仍可能回调一次；旧 op 的 `completed` 若晚到，也不许把新 op 的字段清掉 |
| E-build-03 | 订正原 `:41-42` 的注释 | 原文把泄漏归因于"同名 `EveryName` 会先停掉上一个同名定时器" —— 改成唯一 id 之后**这个原因已不成立**；真正的原因是"被停掉的那个 op 再没有人放行它" |

### 缺陷本质

`op.allowSceneActivation = false` 是"进度 ≥ 0.9 才放行"的门控，而**放行只发生在它自己那条轮询回调里**。每次 `Load` 都会 `Stop` 上一次的轮询 ⇒ 旧 op 既没人再推进度、激活门又关着 ⇒ 永远 `isDone == false` / `isLoaded == false` / `rootCount == 0`（僵尸场景）。累积后编辑器内后续 `LoadSceneAsync` 的 `progress` **恒 0.000**，依赖它的流程（`AppFlow.EnsureMenuScene` 的 `onReady`）永不触发 —— 现象是"主菜单 / 创角屏一个都不开"，且**零报错**。

### 最小复现（实测口径）

编辑器内连续两次 `Game.Scene.Load("Menu")`（= 任何"上一次加载还没完成就再发一次"的序列）：

```
修复前：连续 10 次采样 LoadSceneAsync("Menu").progress = 0.000 / isDone=False（allowSceneActivation=true 也推不动）
        SceneManager.sceneCount = 4（Boot + 3 × Menu，全 isLoaded=False rootCount=0）
修复后：第 2 次 Load 打一条 [Warn] [Scene] 上一次加载未激活就被新的 Load 顶掉（非预期分支）…
        旧 op 被放行后到达 completed，新 op 的 progress 正常推过 0.9
```

### 自证方式（本片口径：**编译 + 代码事实**；实机回归待重启后）

- `git diff --numstat` = `23  2  Runtime/Presentation/Scene.cs`（净 +21 行，最小修复）；
- `unity command recompile`（下游 `clover-project-diablo2/client`）→ `recompile_status = completed / failed=false / errors=[]`
  （该 client 通过 `Packages/manifest.json:3` 的 `file:` 本地包引用本引擎 ⇒ 编译确实覆盖被改文件）；
- **遗留竞态（未改，改动前就有）**：若被顶掉的那个 op 已经过了 0.9（`allowSceneActivation` 已是 `true`、`completed` 已订阅），
  它晚到的 `completed` 仍会把 `_currentScene` 写回旧场景名。

### 首个消费方

`clover-project-diablo2` 收尾轮：该项目的"进图 / 回主菜单"全走 `Game.Scene.Load`；实测现象与修复动机见该项目的 `策划/验收表.md`「收尾轮」节。

---

## E-core-05 ~ E-core-08 · 引擎能力下沉（第一批：通用横切能力，2026-09-19）

**日期**：2026-09-19。**新增文件**：`Runtime/Core/Rng.cs`、`Runtime/Core/LogThrottle.cs`、`Runtime/Core/AStar.cs`、`Runtime/Core/IsoLayout.cs`、`Runtime/Core/Dir8.cs`（**纯新增**；引擎既有文件一行未改）。

> **性质**：这四条不是"修 bug"，而是**能力下沉** —— 把某个业务项目在 `Assets/Scripts/Core/` 里自写、**每个新项目都会重写一遍**的通用横切能力搬进引擎。
> 动机：`skill §7`「写通用设施前先查引擎能力表 —— 引擎有就用引擎的」；实测这些能力（可复现随机 / 日志降频 / 运行时寻路 / 等距几何）在引擎里**根本不存在**，业务只能自写。

| # | 新增 | 能力 |
|---|---|---|
| E-core-05 | `Runtime/Core/Rng.cs` | 注入式**可复现**随机（`System.Random` 内核）：`Next*/NextFloat/Range/Chance/Pick/PickWeighted/Shuffle/NextGrid/Index` + `FromTime/Derive/DeriveSeed`；非法参数**不抛异常**、返回安全值 + **限频告警** |
| E-core-06 | `Runtime/Core/LogThrottle.cs` | 日志防刷屏：`ShouldLog / WarnThrottled / ErrorThrottled / WarnOnce / ErrorOnce / Reset` + 可注入 `Clock`（时钟三级：注入 → `Time.realtimeSinceStartup` → `Stopwatch` 降级，**降级只报一次且直发 `Game.Logger`**，不经 `ShouldLog`） |
| E-core-07 | `Runtime/Core/AStar.cs` | 运行时 A\*（**回调式** `Func<Vector2Int,bool> walkable` ⇒ 零业务类型依赖）：`Find / FindSmoothed / Smooth / HasLineOfSight / Describe` |
| E-core-08 | `Runtime/Core/IsoLayout.cs` + `Runtime/Core/Dir8.cs` | 等距投影 / 排序 / 方向（`sealed` 实例类，构造收 `halfW/halfH/sortOrderStep/sortOrderBase` ⇒ 不绑定任何项目常量）+ 引擎自带 8 方向枚举 |

### 自证

- **引擎侧离线断言**：`Tools~/core-assert/rng-throttle` ⇒ **71/71 ALL PASS**（同 seed 同序列 / 非法参数不抛 / shuffle 确定性 / 限频与只报一次 / 空 key 不刷屏 / 时钟三级降级与恢复）；父宿主 `Tools~/core-assert` 仍 ALL PASS（新增 `Directory.Build.targets` 把子宿主排除出父宿主编译项，父 csproj 一行未改）。
- **业务侧切换（同批完成，`clover-project-diablo2`）**：删 `Core/{Rng,AStar}.cs`、`Core/Iso.cs` 改**薄门面**（静态 API 不变、内部委托 `IsoLayout`）、`Core/Log.cs` 的限频/时钟改为委托 `LogThrottle`（tag 白名单留在项目侧）。判据：
  · **公开签名字符级对照 0 差异**（`Rng` 18 项 / `Log` 17 项 / `AStar` 12 项 / `Iso` 24 项 / `Dir8` 11 项）；
  · `unity command recompile_status` = `completed / failed=false / errors=[]`；
  · **10/10 离线宿主 `FAILED=0`**；
  · **行为等价逐行比对 0 差异**（body 5760 行 + 断言 1136 行 + 地图转储 240 行；先用"同构建重跑"标定抖动基线，再比改前/改后）。
- **已知代价（登记）**：`CloverEngine.Dir8` 与项目 `Diablo2.Def.Dir8` **同名** ⇒ 同时 `using` 两者的 6 个业务文件各加 1 行 `using Dir8 = Diablo2.Def.Dir8;`。**裁决**：保留引擎侧的直观命名（`Dir8`），业务侧用别名；⛔ 不改引擎命名、⛔ 不把枚举塞进子命名空间（会破坏 Core 的命名一致性）。

### 消费方

`clover-project-diablo2`（切换完成，见其 `tools/ai-skill/registry.md`）。后续项目直接取用引擎能力，⛔ 不必再自写这四类设施。

---

## E-core-09 ~ E-core-10 · 引擎能力下沉（第二批：资源的「同步存在性」与「同步批量取」，2026-09-19）

**日期**：2026-09-19。**文件**：`Runtime/Core/Contracts.cs`（接口，**纯新增 2 个成员**）、
`Runtime/Resource/ResourceBackend.cs`（后端契约 + `ResourcesBackend` 实现）、
`Runtime/Resource/AssetBundleBackend.cs`（清单索引实现 + 显式降级）、
`Runtime/Resource/ResourceManager.cs`（按路径缓存 + 转发 + 切后端时清缓存）。

> **性质**：与 E-core-05~08 同批的**能力下沉**（不是修 bug）。业务要「① 同步问一句这条资源在不在」
> 「② 同步一次取一个路径下的**全部**资源（条带 / 图集的子 sprite **按名取不到**）」，而引擎只有**异步**
> `LoadAsset` + 「只取已驻留」的 `TryGet` ⇒ 业务只能直连 Unity 的 `Resources`（绕开资源根前缀 / 缓存 /
> 卸载 / 热更后端）。出处：`clover-project-diablo2` 的 `策划/验收表.md` **E1** 例外行（根因两条正是这两个能力）。

| # | 新增 | 能力 |
|---|---|---|
| E-core-09 | `IResourceManager.Exists(string path)` | **同步**回答「这条路径在可加载源里存不存在」。**只回答**：不占缓存、不动引用计数、不改变 LRU；空路径恒 `false`。后端有索引 ⇒ **查索引**（AssetBundle 查清单，不读盘不开包）；无索引（Resources）⇒ 降级为**一次探测**（`Resources.Load` 主资源 + `Resources.LoadAll` 子资源 —— 少了后一半会把条带/图集误报成「不存在」），上层**按路径缓存**（同一路径只探一次，切后端时清空）。 |
| E-core-10 | `IResourceManager.LoadAll<T>(string path)` | **同步**批量取该路径下的全部资源（**会加载**、同步返回）。**不进**缓存/引用计数（对象由调用方自持，⛔ 不要对它 `Release`）；一个都没有 / 后端不支持 ⇒ **空数组 + Warn**（⛔ 不抛、不静默）。`AssetBundle` 后端**显式不支持**（清单只声明「路径 → 包内**一个**资源名」，没有「路径 → 包内全部资源」的映射）⇒ 空数组 + 只 Warn 一次，**不**回退去猜 Unity 的 `Resources`。 |

### 三个同步入口的语义（必须区分，已逐条写进 XML 注释）

| 入口 | 触发加载？ | 驻留/引用计数 | 用途 |
|---|---|---|---|
| `TryGet<T>(path)`（E3） | **否** | 纯读，不动 | 取**已经装进来**的对象 |
| `Exists(path)`（E-core-09） | Resources 后端会（降级探测）；Bundle 后端不会 | **不驻留**、不动 | 回答「在不在」（配置缺失分支 / 占位色判据） |
| `LoadAll<T>(path)`（E-core-10） | **是**（同步阻塞） | **不驻留**、不动 | 条带 / 图集**整条取**（子 sprite 按名取不到） |

### 缺陷本质

不是 bug，是**能力缺口**：引擎的资源入口只有 `LoadAsset*`（异步）与 `TryGet`（只读已驻留）。
于是「同步问在不在」「同步整条取」这两件小事，业务只能 `Resources.Load*` 直连 ——
一旦直连，资源根前缀、缓存、LRU/水位、卸载策略、热更后端**全部静默失效**（换后端时也不跟着变）。

### 最小复现（改造前编译失败）

业务侧写 `Game.Res.Exists(path);` / `Game.Res.LoadAll<Sprite>(path);` → 编译失败：

```
error CS1061: 'IResourceManager' does not contain a definition for 'Exists' ...
error CS1061: 'IResourceManager' does not contain a definition for 'LoadAll' ...
```

（真实形态就是 `clover-project-diablo2` 里那 4 个 `Resources.Load*` 调用点，见其验收表 E1 的出处列。）

### 修复后自证

- **既有签名零变化（字符级对照）**：`IResourceManager` 成员列表 `19 → 21`，`Compare-Object` 的差异**只有两行**：
  `=> bool Exists(string path);` 与 `=> T[] LoadAll<T>(string path) where T : UnityEngine.Object;`（旧成员一字未动）。
- **下游编译**：`unity command recompile` ⇒ `completed / failed=false / errors=[]`（`clover-project-diablo2/client`，
  经 `Packages/manifest.json` 的 `file:` 本地包引用本引擎）。
- **运行时三态 + 对照（Play，一个会话内）**：
  ```
  ① Exists("D2/Items/invbrx")            = true    ② Exists("D2/Items/invnosuchitem") = false
  ③ Exists("")                           = false   ④ Exists 重复调用同一路径         = true（命中按路径缓存）
  ⑤ LoadAll<Sprite>("D2/UI/Menu/button_wide") = 3 个，名字 button_wide_0/1/2（逐帧按名取不到的那个条带）
  ⑥ LoadAll<Sprite>("D2/Fonts/font42")        = 256（字模图集）；LoadAll<条带不存在> = 0（**空数组，不是 null**）
  ⑦ 对照组 TryGet<Sprite>(同一路径)      = null（未驻留 ⇒ 证明 Exists/LoadAll 不是 TryGet 的换皮）
  ```
- **业务回归**：`clover-project-diablo2` 侧 10/10 离线宿主 `FAILED=0`（新增两条断言：① `Assets/Scripts` 里直连
  `Resources.Load*` **0 命中**；② `Cfg` 读 config.json 确实经 `Game.Res.LoadAll<TextAsset>` 发出），
  进 Play 一轮 `consoleErrors = 0`，且三类原版素材（控制面板/背包石板贴图、`inv*` 物品图标、中英文位图字模）
  观感与改造前**逐项一致**（对照图见该项目 `Assets/Screenshots/p34_ui_res.png`）。

### 已知边界 / 精度限制

- `Exists` 在 **Resources 后端**是「探测」不是「查索引」：代价与旧的 `Resources.Load(x) != null` 相同，
  且上层缓存了答案 ⇒ 同一路径一辈子只探一次；但它**不能**用于「探一次就不加载」的强诉求（Unity 没有这种 API）。
- `Exists == true` **不保证**能同步拿到对象（拿对象是 `TryGet` / `LoadAsset`），也**不保证**加载一定成功
  （Bundle 模式下包文件可能缺失/损坏 —— 那由 `BeginLoad` 那条路报错）。
- `LoadAll` 在 Bundle 后端**恒空数组**（契约内的显式降级），别拿它当「包内枚举」用。
- `LoadAll` 会**同步加载**：别在每帧路径上对大数据调用（条带/图集这类一次性取图才是它的场景）。

### 用法 + 首个消费方

```csharp
// ① 同步问「这张原版图在不在」→ 决定"占位色"而不是让 Image 停在初始白
if (!Game.Res.Exists(ResPaths.ItemIcon("brx"))) { icon.color = MissingIconColor; }

// ② 整条取（子 sprite 按名取不到时才需要它）
var frames = Game.Res.LoadAll<Sprite>(ResPaths.MenuButtonWide);   // 3 帧，名字 button_wide_0/1/2
```

`clover-project-diablo2`（首个消费方，同批切换完成）：`UI/UiArt.cs`（按钮底图取帧兜底）、`UI/D2Text.cs`
（字模整图集兜底）、`UI/D2Icon.cs`（`ItemIconExists` + 缺图占位分支）、`Core/ClientConfig.cs`（读 `config.json`）
⇒ 该项目 `Assets/Scripts` 里直连 `Resources.Load*` 的调用点**清零**，验收表 **E1 例外可移除**。

### 文档同步（本轮只做了引擎侧，其余落点留给下一棒）

| 落点 | 状态 |
|---|---|
| `Runtime/**` 源码注释 | ✅ 本次同步（`Contracts.cs` 三个同步入口的语义表 + 两个后端实现的降级说明） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| 站点文档 `clover-doc/`（`client/reference/api-cheatsheet.md`） | ✅ **已补齐**（A6 轮新增的「数据与通用件」节，`TryGet` / `Exists` / `LoadAll` 三个同步入口的语义差别已写明） |
| skill（`reference/engine-mental-model.md` 能力表） | ✅ **已补齐**（`Game.Res` 行含 `TryGet` / `Exists` / `LoadAll`；A6 轮补） |

---

## E-core-11 · 引擎读不了**自家打表工具的产物** ⇒ 每个业务项目都自己再写一遍配表加载器（2026-09-19）

**日期**：2026-09-19。**新增文件**：`Runtime/Data/CloverTable.cs`（+ `.meta`；**纯新增**，引擎既有文件一行未改）。

> **性质**：与 `E-core-05~10` 同批的**能力下沉**（不是修 bug）：把「读打表产物 tsv + 按主键强类型取行」搬进引擎。
> 编号来源：续本文件既有「能力下沉」系列（`E-core-05~08` = Rng/LogThrottle/AStar/IsoLayout+Dir8、
> `E-core-09~10` = 资源同步入口），`E-core-11` 此前未被占用。

| # | 新增 | 能力 |
|---|---|---|
| E-core-11 | `Runtime/Data/CloverTable.cs` | ① `LoadAll(streamingAssetsDir, dataDir)`：多候选目录解析（`<sa>/Table` → `sa` 本身 → `<dp>/Scripts/Table/Tsv` → `dp` 本身）+ 调用方声明的必需表校验（缺哪张报**哪个文件**）+ 逐 `*.tsv` 读取建两个主键索引；**失败返回可定位错误串，⛔ 不抛异常、⛔ 不静默**；② `Get<T>(tableName, int)` / `Get<T>(tableName, string)`：取不到 ⇒ `null` + **限频告警**（不崩）；③ `Dir` / `ResolveDir` / `RequiredTables` |

### 缺陷本质（能力缺口）

引擎数据域只有 `CloverData.InitDataTable(dir)` + `IDataTable.Load<T>(fileName, parser)`，而它的契约要求行类实现
`IDataRow`（`Runtime/Core/Contracts.cs`，`int Id { get; }`）；**打表工具生成的行类是普通字段容器**
（[`clover-tools/table/core/internal/gen/cs.go:259-267`](https://github.com/qw576483/clover-tools/blob/main/table/core/internal/gen/cs.go:259-267)），既不实现 `IDataRow`、主键也不一定是 `int`
（`treasureclass_c` 的主键是 `string`）⇒ **引擎自带的那条链路对打表产物完全用不上**。
于是每个业务项目都只能自己再写一份「找目录 / 读 tsv / 报错 / 建索引」的加载器
（`clover-project-diablo2` 原先那份 = `client/Assets/Scripts/Table/TableLoader.cs`，114 行），
且各写各的 ⇒ 目录约定、报错质量、边界处理（表缺失 / 主键不存在）在每个项目里都要重踩一遍。

### 与打表产物**逐条对齐**的约定（改任一条 ⇒ 值与生成代码分叉）

| 约定 | 出处 |
|---|---|
| tsv：第 1 行列名表头、第 2 行起数据、`File.ReadAllLines` + `'\t'` 分隔、**只跳空行**（⛔ 不认 `#` 注释） | [`clover-tools/table/core/internal/gen/cs.go:277-285`](https://github.com/qw576483/clover-tools/blob/main/table/core/internal/gen/cs.go:277-285) |
| 主键 = **第 1 列**（int 主键取整数解析、string 主键取原样字符串） | `cs.go:311` + `cs.go:137-142`（`csPkType`） |
| 列名 → 行类字段名：按非字母数字拆段、每段首字母大写拼接（`hp_min`→`HpMin`、`is_act1_start`→`IsAct1Start`） | [`clover-tools/table/core/internal/gen/ident.go:15`](https://github.com/qw576483/clover-tools/blob/main/table/core/internal/gen/ident.go:15)（`ToGoIdent`） |
| 单元格 → 值：空/非法取类型默认值**不报错**；`map` 用 `\|`+`;`（兼容 `:`）、slice 用 `;`、vector3 用 `;`/`,` | `cs.go:159-246`（`TableParsers.*`） |
| 重复主键**后者覆盖前者**；行序 = 文件行序 | `cs.go:311` |
| 数值解析**不加 `InvariantCulture`**（生成代码用当前区域设置，引擎必须一致） | 同上 |

### 最小复现（改造前编译失败）

业务侧写一行 `CloverEngine.CloverTable.LoadAll(dir);` → 编译失败：

```
error CS0103: 当前上下文中不存在名称"CloverTable"
```

（真实形态就是每个项目各自那份加载器；`clover-project-diablo2` 的那份已在本轮改为**薄转发**。）

### 自证（离线，真跑；证据口径：**未跑 Unity 实机**，只有编译 + 离线断言）

一次性自检工程 `.ai-tmp/test/_a4/tabledump`（把 `Runtime/Core/LogThrottle.cs` + 本文件 + 项目 `Table/**`
一起编到 .NET，跑完即删）：

| 场景 | 命令 | 结果 |
|---|---|---|
| 引擎路径 vs 生成壳路径 | `dotnet A4TableDump.dll dump` | 10 张表 **821 行全部命中**（`hits=N/N`），**逐字段 DIFF=0**；不存在的 int/string 主键各探一次 ⇒ `null` + 限频告警 |
| 改前净效果 vs 改后门面 | `gendump` 与 `dump` 的 A 行 | **831 行（全部行 × 全部字段）逐行 0 差异** |
| 失败路径 | `dotnet A4TableDump.dll fail` | `LoadAll(不存在目录)` ⇒ 返回**可定位错误串**（4 个候选路径，非 null、非异常）；其后 `Get` ⇒ `null` + 一条限频告警（**第二次调用不再打日志**）；换回正确目录 ⇒ 恢复成功 |
| 下游编译 | `unity command recompile_status`（`clover-project-diablo2/client`） | `completed / failed=false / errors=[]` |
| 下游回归 | 10 个离线宿主 | `TOTAL_HOSTS=10 FAILED=0`；掩膜「墙体时钟地图层」（seed 由 `Rng.FromTime` 派生）后，**10/10 宿主逐行 0 差异**（且掩膜投影在同构建重跑下字节相同） |

### 已知边界 / 精度限制（防当万能药用）

- `Get<T>` 用**反射**填 `T` 的 public 字段（打表行类在业务程序集里，引擎编译期不认识它）——
  这与「打表产物自身不用反射」（`clover-client-unity-engine-index.md` §3.3「DataTable …不用反射」）**不冲突**：
  生成代码仍是零反射，反射只在引擎这一层、且**按 (表, 行类型, 列) 缓存**。`T` 必须满足
  「**public 无参构造 + public 字段**」，否则返回 `null` + 一条 Error 级限频日志。
- 支持的类型 = 打表工具会产出的那一组（`int` / `long` / `float` / `string` / `int[]` / `string[]` /
  `Dictionary<int,int>` / `Dictionary<int,string>` / `Vector3` 形状的三 `float` 结构）；
  其它类型**不填值** + 限频告警（⛔ 不假装填成功）。
- `Get<T>(table, int)` 只对 **int 主键**的表有意义，`Get<T>(table, string)` 只对 **string 主键**的表有意义
  （用错重载 ⇒ 取不到 ⇒ `null` + 限频告警）。
- **成功路径不打日志**（业务侧自己打；引擎打会让每个宿主/每次启动多一行噪声）；失败/取不到才留痕。
- **失败不破坏**上一次成功加载的数据（与生成壳「只在成功时整体替换」一致）。
- 多候选目录是**顺序命中、目录存在即采用**（与既有业务加载器逐条一致）；「同名表两份」保留先读到的那份 + 限频告警。
- 非线程安全（主线程使用）。

### 用法 + 首个消费方

```csharp
string err = CloverTable.LoadAll(Game.Res.StreamingAssetsDir, Game.Res.DataDir);  // 失败 ⇒ 可定位错误串
if (err != null) Game.Logger?.Error("Table", err);
var monster = CloverTable.Get<BaseMonsterRow>("Monster", 1);   // 表名 = tsv 文件名去扩展名
```

`clover-project-diablo2`（首个消费方，同批切换完成）：`client/Assets/Scripts/Table/TableLoader.cs`
改为**薄转发**（`LoadAll` 转发引擎 + 把打表自带的强类型壳 `Tables.Default` 灌上；10 个便捷访问器转发
`CloverTable.Get<T>`；**公开签名与公开常量一字未改**），6 个链接了 `Table/**` 的离线宿主补一行引擎链接项。

### 文档同步（本轮只做了引擎侧，其余落点留给下一棒）

| 落点 | 状态 |
|---|---|
| `Runtime/**` 源码注释 | ✅ 本次同步（`CloverTable.cs` 文件头写全「与打表产物逐条对齐的约定 + 出处 + 语义约束 + 已知边界」） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` §3.3 / `结构规则.md` | ✅ **已补齐**（§3.3 新增 `CloverTable` 行；`结构规则.md` 的 Data 域「允许放入」已收录；A6 轮补） |
| 站点文档 `clover-doc/`（`client/reference/api-cheatsheet.md`） | ✅ **已补齐**（新增「数据与通用件」节的 `CloverTable` 小节；A6 轮补） |
| skill（`reference/engine-mental-model.md` 能力表 + `patterns/table.md`「代码侧怎么读」） | ✅ **已补齐**（能力表 ★ `CloverEngine.CloverTable` 行；`patterns/table.md`「代码侧怎么读」的客户端段已改为「加载用 `CloverTable.LoadAll`、取行两条路」；A6 轮补） |

---

## E-core-12 · 引擎通用件的 `Text` 一律走内置字体 ⇒ 像素风业务**没法让引擎自己的提示也走自己的字模**（2026-09-19）

**日期**：2026-09-19。**文件**：`Runtime/Presentation/TextHooks.cs`（**纯新增** + `.meta`）、
`Runtime/Presentation/UIWidgets.cs`（`UIFactory.CreateText` 末尾**新增一行**调用 + 该方法的 XML 注释同步）。
引擎既有公开签名**一个都没动**。

> **性质**：与 `E-core-05~11` 同批的**能力下沉**（不是修 bug）：把「引擎建完 `Text` 之后让业务接管渲染」开成正式接口。
> 业务侧（自带位图字模的项目）此前**没有任何入口**能把引擎通用件里的中文换成自己的字模 —— 通用件是唯一一处
> 「业务改不到、又确实显示中文」的地方（实测：`clover-project-diablo2` 验收表 **E19**：
> 引擎 Toast 的「角色名已存在」「背包已满」与 LoadingLayer 的「加载中...」全是系统 TTF）。

| # | 新增 | 能力 |
|---|---|---|
| E-core-12 | `ITextHook` + `TextHooks.Current` / `NotifyCreated(Text)` | 引擎**所有**创建 uGUI `Text` 的位置（当前唯一一处 = `UIFactory.CreateText`，Toast / Loading / 确认框 / 飘字 / 引导 / 按钮文字全经它）在 Text 就位后通知业务一次，业务可把自己的渲染器挂上去。未注册 ⇒ 判空即返回（**与没有本接口时逐字等价**）；挂钩抛异常 ⇒ 吞掉 + Warn 一次（⛔ 不让通用件打不开） |

### 缺陷本质（能力缺口）

`UIFactory.DefaultFont()` 是引擎给「零美术依赖也能显示文字」的兜底字体（Unity 内置 `LegacyRuntime.ttf`）。
业务面板的文字业务自己说了算，但通用件（Toast / Loading / 确认框 / 飘字 / 引导）是引擎**自己代码搭的**，
`Text.font` 在引擎里写死 ⇒ 业务侧只能「扫一遍源码看有没有漏网」，**没有任何合法接管点**。
对像素风项目这就是硬伤：画面上其余像素全来自原版素材，只有引擎提示是系统 TTF（一眼可见的破格）。

### 最小复现（改造前编译失败）

业务侧写 `CloverEngine.TextHooks.Current = myHook;` → 编译失败：

```
error CS0103: 当前上下文中不存在名称"TextHooks"
```

（真实形态就是 `clover-project-diablo2` 的 `UI/D2EngineTextHook.cs` 与 `App/Bootstrap.cs` 那两行。）

### 修复后自证

- **既有行为零变化（结构判据）**：`UIFactory` / `UIWidgets` 公开签名逐条未改；注入点只有
  `CreateText` 末尾一行 `TextHooks.NotifyCreated(text);`（离线断言：位于 `supportRichText = false;` 之后、
  `return text;` 之前，全文件只出现 1 次；且 `Runtime/**` 里 `AddComponent<Text>()` 全仓唯一 = `UIWidgets.cs`）。
- **未注册 ⇒ 逐字等价**（本片最关键）：`TextHooks.Current == null` 时 `NotifyCreated` 只做一次判空即返回
  （不分配 / 不写 Text / 不打日志）。三层证据：
  ① 离线：10/10 离线宿主与改动前输出**逐行 0 差异**（`TOTAL_HOSTS=10 FAILED=0`）；
  ② 离线：把**真实** `TextHooks.cs` 编到 .NET 跑语义自检 **13/13 PASS**（默认 `null` / 未注册零副作用 /
     抛异常被吞且只 Warn 一次 / 换挂钩重置「只报一次」/ 引用相等 / 清空后不再回调 / 注入点位置）；
  ③ **实机（同一会话内对照）**：同一条创建路径 `UIFactory.CreateText("P36Probe…", <canvas>, "角色名已存在", 24, …)`
     跑两次 —— `Current = null` ⇒ `enabled=1 font=LegacyRuntime mirror=0`（= 改造前行为，E19 判据命中 1）；
     挂钩就位 ⇒ `enabled=0 font=(null) mirror=1 children=[Glyphs(6)]`（Text 不再绘制，画面由 6 个位图字模方块产生）。
- **E19 消除的机检数字**（判据沿用片 3 的「启用 + 有字体 + 含非 ASCII 的 uGUI Text」扫描）：
  实机 `TEXTSCAN note=settled … drawnNonAscii=0`（`totalTexts=7 mirrored=7 mirroredStillDrawing=0`）；逐节点：
  `[UI]/System/Loading/SafeArea/Label enabled=0 font=(null) mirror=1 text="加载中..." children=[Glyphs(6)]`、
  `[UI]/Top/Toasts/Toast/Label … "角色名已存在" children=[Glyphs(6)]`、`… "背包已满" children=[Glyphs(4)]`、
  `engineTexts=3 stillDrawnByUgui=0`。
- **表现类证据**：`clover-project-diablo2/client/Assets/Screenshots/p36_engine_text.png`（1920×1080，`--source screen`）
  —— 主菜单上同时可见引擎 Toast（「角色名已存在」「背包已满」）与 LoadingLayer（「加载中...」+ 指示器），
  三处都是**原版字模**；数值化旁证：Toast 文本区 1751 个墨迹像素**只有 4 种 RGB**（TTF 抗锯齿会给出上百种灰阶）。
- **实机零错误**：`console_status` ⇒ `consoleErrors = 0`（10 条 Warn 全是既有噪声：编辑器 DeviceId / MapRoot 空 /
  NPC 站位未生成 / 退出时资源引用计数），无 `bitmap.unavailable`、无 `Game.Res 未初始化`。

### 已知边界 / 精度限制（防当万能药用）

- 挂钩是**进程级静态**，且只通知「创建后一次」：引擎**不会**在之后的文案变更时再通知
  （业务要在 `text.text` 变化时跟画，得自己逐帧读数据 —— 本项目用 `D2TextMirror.Update` 的「变了才重画」）。
- `NotifyCreated` **不判空**（`text` 可能为 null）：引擎侧注入点永远传真实 Text，判空留给挂钩自己
  （这样「挂钩抛异常」这条路径在任何入参下都能被完整验证）。
- 通知时机 = `CreateText` 的**最后一行**：挂钩拿得到文案 / 字号 / 对齐 / 颜色 / 溢出策略，但**拿不到**调用方
  随后做的布局（`CreateButton` 的 `Stretch`、`UiArt.Button` 的 `fontSize = 20` 都在通知之后）。按布局决策的挂钩
  必须自己逐帧跟（本项目只看字号 ⇒ 不受影响）。
- 挂钩内部**不许再调 `UIFactory.CreateText`**（会再触发一次通知；本项目靠重入闸门兜住，
  ⛔ 但这是「防炸」，不是「鼓励这么写」）。
- ⚠️ **业务侧还有一条硬前提**：挂上真实渲染器之前必须确认资源模块已就绪。本项目实测：
  `D2Text.EnsureChi` 在 `Game.Res == null` 时走 `MarkBitmapUnavailable` = **整个项目的字模永久降级成系统字体**
  （一局内不再恢复）；而引擎 LoadingLayer 的 `Text` 恰在 `Game.Launch` 期间建（那时 `CloverRes.Init` 还没调）
  ⇒ 挂钩侧必须先**寄存**、等业务在 `CloverRes.Init` 之后补挂（本项目做法见其 `App/Bootstrap.cs` 的
  `Install()` / `FlushPending()` 两行）。实机日志：
  `文字渲染挂钩：资源模块尚未就绪（Game.Res == null）⇒ 寄存` → `资源模块就绪后补挂 1 条引擎 Text`。

### 用法 + 首个消费方

```csharp
// 业务侧（引擎启动前注册；实现见 clover-project-diablo2 的 UI/D2EngineTextHook.cs）
TextHooks.Current = new MyTextHook();   // 例：OnTextCreated 里把 Text 改成"数据持有者" + 挂自己的字模渲染器
// 引擎侧（唯一注入点，业务不用管）
var text = UIFactory.CreateText(...);   // 建完自动通知 TextHooks.Current
```

`clover-project-diablo2`（首个消费方）：`App/Bootstrap.cs`（`Install()` 在 `Game.Launch` 之前、
`FlushPending()` 紧跟 `CloverRes.Init`）+ `UI/D2EngineTextHook.cs`（`ITextHook` 实现；三道闸门：
重入 / 祖先已镜像 / 资源未就绪）⇒ 该项目验收表 **E19 可移除**（机检 `drawnNonAscii=0`）。

### 文档同步（本轮只做了引擎侧，其余落点留给下一棒）

| 落点 | 状态 |
|---|---|
| `Runtime/**` 源码注释 | ✅ 本次同步（`TextHooks.cs` 文件头写全「为什么 / 生命周期 / 契约 / 异常约定」；`CreateText` 的 XML 注释同步说明「这是唯一注入点」） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` / `结构规则.md` | ✅ **已补齐**（`index` 的 Presentation 目录树含 `TextHooks`；`结构规则.md` §3.5 表现域「允许放入 · 界面」已收录；A6 轮补） |
| 站点文档 `clover-doc/`（`client/reference/api-cheatsheet.md` + 客户端开发那篇的 UI/通用件一节） | ✅ **已补齐**（`api-cheatsheet.md` 的「UI 构件」节已登记 `TextHooks.Current` / `ITextHook.OnTextCreated`；A6 轮补） |
| skill（`reference/engine-mental-model.md` 能力表「UI」行） | ✅ **已补齐**（能力表 ★ `CloverEngine.TextHooks` 行；A6 轮补） |

---

## E-core-13 · 引擎只有「单文件 KV」⇒ 每个项目都自己重写一遍「一槽一文件」的原子写 + 损坏留档 + 枚举（2026-09-19）

**日期**：2026-09-19。**新增文件**：`Runtime/Data/FileSlotStore.cs`（+ `.meta`；**纯新增**，引擎既有文件一行未改）。

> **性质**：与 `E-core-05~12` 同批的**能力下沉**（不是修 bug）：把「一槽一文件」的落盘层搬进引擎。
> **编号**：本条占用 `E-core-13`（此前全文未用过）。
> ⚠️ 与本轮在途的 `LogBuffer`（`Runtime/Core/LogBuffer.cs`，其能力表标着「E 号：待主 agent 统一编」）
> **不冲突** —— 后者尚未编号，建议顺延为 `E-core-14`。

| # | 新增 | 能力 |
|---|---|---|
| E-core-13 | `Runtime/Data/FileSlotStore.cs` | 「键 → 文本」的**槽位**存储，**一槽一文件**：`Exists` / `Write(key, content, out error)` / `Read(key)` / `Delete(key)` / `List()`（**字典序，跨进程一致**）/ `LastCorruptPath`。写盘 = 原子写（`<key><ext>.tmp` → `File.Replace`，目标不存在 / 平台不支持 ⇒ 删除 + 改名兜底）；坏内容 ⇒ `Read` 返回 `null` + 留档 `<key><ext>.corrupt`（**副本**语义、原文件不丢）+ 限频告警一次；`Write` 遇到目标已是坏文件 ⇒ **先留档再写**。`key` 非法（空 / 含 `/` `\` / 非法文件名字符）⇒ `false` + 可定位错误串，⛔ 不抛、⛔ 不允许 `../` 逃出槽目录 |

### 缺陷本质（能力缺口）

引擎数据域只有 `Setting`（`Runtime/Core/Setting.cs`）—— **单文件 JSON、键值对**：一个 `settings.json` 里一个键存一份文本，设置在内存里攒、`Save()` 一次写全。想做「一只角色一个文件 / 一局回放一个文件 / 一章关卡草稿一个文件」的项目，只能自己再写一遍「先写 `<f>.tmp` → `File.Replace` 原子替换 + 坏文件留档 + 目录枚举」—— 每个新项目重写一次、写法还各不相同。

`ISetting` 也**没有"列出全部 key"**的接口 ⇒ 项目侧只能自己再维护一个「索引键」来记住有哪些槽（`clover-project-diablo2` 原先就是 `char/index`）。

### 与 `Setting` 的关系（互补，⛔ 不是替代）

| | `Setting`（E5 起已原子写） | `FileSlotStore`（本条） |
|---|---|---|
| 形态 | 单文件 KV | **一槽一文件** |
| 落盘时机 | 内存里攒，`Save()` 一次写全 | 每次 `Write` 独立落盘 |
| 枚举 | ❌ 无 | ✅ `List()`（字典序） |
| 损坏处置 | 整份 `settings.json` 退默认 + `.corrupt` | **按槽**退化（只坏那一个槽） |

原子写 / 留档 / 失败限频三段是**照搬 `Setting` 的成熟写法**（出处写在文件头：`Setting.cs:349-380` `ReplaceAtomically`、`:382-396` `BackupCorrupt`、`:411-421` 失败限频），⛔ 未另创一套。

### 最小复现（改造前编译失败）

业务侧写一行 `new CloverEngine.FileSlotStore(dir).Write(key, json, out var err);` → 编译失败：

```
error CS0246: 未能找到类型或命名空间名"FileSlotStore"
```

（真实形态就是每个项目各自那份「写 `.tmp` + `File.Replace` + 留档」的私有实现。）

### 自证（**本片未跑编译、未跑 Unity 实机** —— 口径务必照此理解）

> ⚠️ 本轮按用户明确要求「不用测试」执行 ⇒ 下面**只有静态核对**，**没有**编译输出 / 运行时输出 / 断言。
> 凡读成"已在编辑器 / 设备上验证通过"的都是**错的**。

| 项 | 依据（可复跑） |
|---|---|
| 纯新增 | `git -C <仓库根> status --porcelain -- clover-client-unity-engine` ⇒ 只有 `?? Runtime/Data/FileSlotStore.cs`（+ `.meta`）；`Runtime/**` 既有文件本片一行未改 |
| 不依赖 UnityEngine | 文件头 `using` 只有 `System` / `System.Collections.Generic` / `System.IO` / `System.Threading`；`.json` 合法性用引擎自带 `MiniJson.Parse` ⇒ 离线宿主可直接把本文件链进 .NET 工程 |
| 语义逐条落位 | 文件头「语义约束 ①~⑧」逐条对应实现：① `Write` 失败返回可定位 `error`（`:162-210`）；② `Read` 坏内容 ⇒ `null` + 留档 + `WarnOnce`（`:248-255`）；③ 先留档再写（`:189-190` 调 `ArchiveIfCorrupt`，定义 `:436-456`）；④ 目录不可用 ⇒ 三条路径都失败（`:178-183` / `:229-233` / `:290`）；⑤ key 非法 ⇒ 三条路径都拒（`:164-169` / `:223-227` / `:266`）；⑥ 无 Unity 依赖；⑦ WebGL 不建目录 / 不读写（`:85-92` / `:357`）；⑧ 非线程安全（类注释） |
| 公开成员 | `Dir` / `LastCorruptPath` / `FileSlotStore(string dir, string extension = ".json")` / `Exists` / `Write` / `Read` / `Delete` / `List` |

### 已知边界 / 精度限制（防当万能药用）

- `List()` 是**字典序**（`StringComparer.Ordinal`），⛔ **不是插入序**：要「按创建先后」显示（如选角屏卡片顺序）**自己维护索引键** —— 这是与 `Setting` 的**刻意分工**，不是缺陷。
- 坏内容判定**按扩展名**：默认 `.json` 才要求内容能被 `MiniJson.Parse` 解析（解析抛异常 / 内容空白 ⇒ 判坏）；**其它扩展名一律视为合法文本**（引擎没法定"任意文本里什么叫坏"，⛔ 不猜）。
- `Delete` **不删** `.corrupt` 留档 —— 那是坏内容的现场，属证据。
- 目录**不在构造时创建**：第一次成功 `Write` 才建（与 `Setting.Save` 同款懒创建），构造不许因为"目录暂时不存在"就废弃整个存储。
- 失败上报**降频**：第 1 次必报、之后每 100 次一条（与 `Setting` 同口径）。
- `Read` 遇坏内容会**留档 + 返回 `null`**（按"没有这个槽"处理）⇒ 调用方如果把它当"空档"直接回写，等于用新内容覆盖坏档；但现场副本已在 `.corrupt` 里，可事后取证。
- **槽位键 = 文件名** ⇒ 调用方若拿**用户输入**当 key（如角色名），必须保证它能当文件名（⛔ 不能含 `/` `\` `:` `*` `?` `"` `<` `>` `|` 等）。引擎在 key 非法时**返回失败 + 可定位错误串**（⛔ 不抛、⛔ 不静默），但**不会替调用方转义**。
  首个消费方的处置（**已收尾**）：`clover-project-diablo2` 的创角屏把键入字符收紧为「字母 / 数字 / 中文 / `_` / `-`」（`CharCreatePanel.IsNameCharAllowed`：非预期字符**拒绝 + 只报一次 Warn**），并在「确定」提交前用 `FirstDisallowedNameChar` 再兜一道（覆盖"`Cfg.DefaultPlayerName` 被配成非法字符"这类绕过键入的路径）。⇒ 该项目正常流程下不会再出现"名字存不下"。

### 用法 + 首个消费方

```csharp
var store = new FileSlotStore(Path.Combine(Game.Config.SettingDir, "saves"));
if (!store.Write("hero_01", json, out var err)) Game.Logger?.Error("Save", err);
var text = store.Read("hero_01");          // 不存在 / 坏了 ⇒ null
var all  = store.List();                   // 字典序（跨进程一致）
var archived = store.LastCorruptPath;      // 最近一次留档的坏文件（没有 ⇒ null）
```

`clover-project-diablo2`（首个消费方）：`client/Assets/Scripts/Module/Save/SaveModule.cs` 的落盘 / 读盘层改为转发本类（**一角色一文件** `<SettingDir>/saves/<角色名>.json`；**公开 API 一字未改**，12 个成员逐个保留）。项目侧的分工与迁移：

- `char/index` **只留创建先后**（选角屏卡片顺序）—— 因为 `List()` 是字典序，而"创建先后"是业务语义；
- `char/{角色名}` **只留旧档懒迁移**：读取时槽位没有才回退旧键，**写成功才删旧键**；`Delete` 同时清旧键（否则删掉的角色下次会"复活"）。

⚠️ 该消费方的「存档往返等价 / 跨进程 `List()` 顺序一致 / 损坏留档有据」三条验收，**本轮未做**（用户明确要求不测试）⇒ 属**未决**：切到槽位后**首次真机进游戏**要复核一次「老档能读 + 新档能存 + 选角屏顺序不变」。

### 文档同步（本轮落点，逐条如实）

| 落点 | 状态 |
|---|---|
| `Runtime/**` 源码注释 | ✅ 本片新增文件自带（文件头「为什么 / 与 `Setting` 的差异 / 语义约束 ①~⑧ / 用法」+ 各方法 XML 注释） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md`（Data 域目录树 + §3.3 数据域表） | ✅ 本次同步 |
| `结构规则.md`（Data 域「允许放入」+ `Setting` 归属说明） | ✅ 本次同步 |
| 站点文档 `clover-doc/`（`client/reference/api-cheatsheet.md` + `client/development/game-facade.md` 的 `Game` 门面表） | ✅ 本次同步（速查表新增「数据与通用件」节并登记 `FileSlotStore`；顺带补齐此前漏登记的 `Rng` / `LogThrottle` / `AStar` / `IsoLayout` / `CloverTable`） |
| skill（`reference/engine-mental-model.md` 能力表；**仓库副本 + 全局 `~/.codebuddy/skills/ai-skill/` 两份都改**） | ✅ 本次同步（新增 `FileSlotStore` 行） |
| 项目侧（`clover-project-diablo2`） | ✅ `SaveModule` 切换 + `GameConst` / `SaveJson` / `Module/Contracts.cs`（注释）/ `CharRoster` / `AppFlow` 文案同步 + `tools/ai-skill/registry.md` 登记 + `UI/CharCreatePanel` 名字字符集收紧（消除"槽位键 = 角色名"带来的边界） |

> 顺带把 `E-core-09~12` 遗留的 ⏳ 落点一次性补完（三处 ⏳ → ✅，见上文各自小节的「文档同步」表）：
> `Exists` / `LoadAll`（E-core-09/10）、`CloverTable`（E-core-11）、`TextHooks`（E-core-12）现均已进
> `clover-doc` 速查表 / 引擎 `index` / skill 能力表。

---

## E-core-14 · 引擎只有「时间间隔」降频 ⇒ 每个项目都自己重写一遍「每 N 次」计数口径（2026-09-19）

**日期**：2026-09-19。**改动文件**：`Runtime/Core/LogThrottle.cs`（**纯扩展**，既有 `ShouldLog` / `WarnThrottled` / `ErrorThrottled` / `WarnOnce` / `ErrorOnce` 一字未改）。

> **性质**：能力下沉（不是修 bug）。
> **编号**：本条占用 `E-core-14`（上一节 `E-core-13` 已预判此编号）。
> **来源项目**：`clover-project-cs16` 的 `client/Assets/Scripts/Module/Player/CsModuleLog.cs`（78 行，被 12 个文件使用）。

| # | 新增 | 能力 |
|---|---|---|
| E-core-14 | `Runtime/Core/LogThrottle.cs` | **计数口径**（与既有时间口径**并存、不可互换**）：`ShouldLogEvery(key, everyN)` / `InfoCounted` / `WarnCounted` / `ErrorCounted`。每 key 独立计数，**第 1 次必打**，之后 `count % everyN == 0` 才打，行尾补 `（同类第 N 次）`（全角括号）；空/null key **归并为 `"default"`**（⛔ 与时间口径的 `ShouldLog` 空 key 语义**刻意不同**，后者是"恒 `false` + 只报一次 Warn"）；`everyN <= 1` 视为 1；`Suppress` 命中则一律 `false` 且不计数。`Reset()` 改为**一并清空两种记录** |

### 缺陷本质（能力缺口）

`LogThrottle`（E-core-06）只有**时间间隔**口径。而高频分支的真实需求常常按**次数**采样而非按秒："首次必打 + 之后每 50 次打一条" —— 与帧率无关、可复现。

`clover-project-cs16` 因此又写了一份 `CsModuleLog`（自持 `Dictionary<string,int>` + `switch` 分发），正是 `结构规则.md` §4.4 说的"已有同类能力不准再起第二套"。

### 与时间口径的关系（互补，⛔ 不是替代）

| | 时间口径（E-core-06） | 计数口径（本条） |
|---|---|---|
| 闸门 | 同一 key 在 `intervalSeconds` 内只出第一条 | 每 key 第 1 次 + 之后每 N 次 |
| 适用 | "每帧都可能发生、一秒说一次就够" | "按发生次数采样，与帧率无关" |
| 空 key | 恒 `false` + **只报一次** Warn | **归并 `"default"`** 继续计数 |

### 项目侧

`CsModuleLog` 降级为**薄适配**（78 → 54 行）：删掉自己的计数表与 `switch`，`Info/Warn/Error` 转发 `LogThrottle.*Counted`，`Always` 转发 `Game.Logger.Info`，`Reset()` 转发 `LogThrottle.Reset()`。**调用面（类名 / 命名空间 / 公开签名 / tag 实例）一个都没动**，12 个调用文件一行未改（已用 SHA256 + mtime 逐个对照证明）。

### 判据

- 离线断言宿主：计数口径 11+ 条边界全 `ALL PASS`（`everyN=0` / `int.MinValue` / `Suppress` / 空 key 归并 / 两口径互不污染 / `Reset()` 两者都清）；
- 既有断言宿主 `Tools~/core-assert/rng-throttle` **27 条全 PASS**（时间口径零漂移）；
- 编译：`compile-check-client.ps1` → `RESULT: ALL PASS` / `GATE_EXITCODE=0`。

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/Core/LogThrottle.cs` 顶部块注释 + 类摘要 | ✅ 本片（出处 / 未下沉什么 / 为什么 / 语义约束） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` §1 目录总览 / §2 模块总览 / §3.4 基础域表 | ✅ 本次同步 |
| `结构规则.md` §3.1 Core「允许放入」/ §4.4 必查能力清单 | ✅ 本次同步 |
| skill（`reference/engine-mental-model.md` 能力表 `LogThrottle` 行） | ✅ 本次同步（补计数口径 + "两种口径不可互换"） |

---

## E-core-15 · 引擎 `ILogger` 只写不读 ⇒ 每个项目都自己写一遍「最近 N 行」环形缓冲（2026-09-19）

**日期**：2026-09-19。**新增文件**：`Runtime/Core/LogBuffer.cs`（+ `.meta`；**纯新增**，`Logger.cs` / `ILogger` 一行未改）。

> **性质**：能力下沉（不是修 bug）。
> **编号**：本条占用 `E-core-15`。
> **来源项目**：`clover-project-cs16` 的 `client/Assets/Scripts/UI/InGame/CsLogBuffer.cs`（123 行，**零 `Cs*` 引用**）。

| # | 新增 | 能力 |
|---|---|---|
| E-core-15 | `Runtime/Core/LogBuffer.cs` | 运行时日志**环形缓冲**：`Capacity`（默认 400，`<=0` 视为默认）/ `Version`（内容变化自增，供 UI 判"要不要重画"）/ `Lines`（`IReadOnlyList<string>` 只读窗口，G8）/ `Installed` / `Install(capacity)`（幂等，挂 `Application.logMessageReceivedThreaded`）/ `Uninstall()` / `Drain()`（返回"自上次 Drain 起新增行数"并归零）/ `Push(text)`（本地回显，也计入 `Version`）/ `Clear()`。超过容量丢最旧；入队**线程安全**（回调来自任意线程），`Version`/`Lines`/`Drain` **只保证主线程读** |

### 缺陷本质（能力缺口）

`ILogger`（`Runtime/Core/Logger.cs:52-95`）只有 `Debug/Info/Warn/Error/Fatal` —— **只有写、没有读**：没有事件、没有订阅、没有"最近 N 行"。

任何项目想做"游戏内控制台 / 排障面板"（尤其**打包后**、没有 Editor Console 时），都得自己再写一遍环形缓冲 + 版本号 + `logMessageReceivedThreaded` 的线程搬运。

### ⛔ 这不是第二套 logger

它**不收日志、不写日志**，只提供"最近 N 行的只读窗口"；`ILogger` 与 `Logger` 一行未动。判据：把它删掉，日志照写不误（只是没人能读最近 N 行）。

### 项目侧

删除 `UI/InGame/CsLogBuffer.cs` + `.meta`，`ConsolePanel.cs` 的 4 处调用改指引擎 `LogBuffer`（`PushLocal` → `Push`）；`[RuntimeInitializeOnLoadMethod]` 的装卸时机**原样搬进引擎**，启动顺序一行未改。

### 判据

- 项目侧 `CsLogBuffer` 残留命中 = **0**（`client/**`，排除 Unity 生成的 `Library/`）；
- `Lines` 声明为 `IReadOnlyList<string>`（反射断言）；`Install` 两次只挂 1 个钩子 / `Uninstall` 可重复调且内容不清 / 后台线程 1000 行全落不丢 / 环覆盖丢最旧 —— 均 `PASS`；
- 编译：`compile-check-client.ps1` → `RESULT: ALL PASS` / `GATE_EXITCODE=0`（`Core` 文件数 26 → 27）。

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/Core/LogBuffer.cs` 顶部块注释 | ✅ 本片（出处 / 未下沉什么 = "面板怎么画" / 为什么 / 语义约束） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` §1 / §2 / §3.4 / §4 门面 | ✅ 本次同步（§4 写明**不经 `Game` 门面**，静态类直接用） |
| `结构规则.md` §3.1 / §4.4 | ✅ 本次同步 |
| skill（`reference/engine-mental-model.md`） | ✅ 本次同步（新增 `LogBuffer` 行） |

---

## E-core-16 · 通用 uGUI 控件在项目里被造了三遍 ⇒ 控件工厂下沉进 `UIFactory`（2026-09-19）

**日期**：2026-09-19。**新增文件**：`Runtime/Presentation/UIWidgetControls.cs`（+ `.meta`）。**改动**：`Runtime/Presentation/UIWidgets.cs` 的 `UIFactory` 改为 `public static partial class`（既有 8 个方法一字未改）。

> **性质**：能力下沉（不是修 bug）。
> **编号**：本条占用 `E-core-16`（沿用既有 `E-core-NN` 连号，不因程序集在 `Presentation` 而另起系列）。
> **来源项目**：`clover-project-cs16` 的 `UI/Flow/CsUiStyle.cs` ＋ `UI/InGame/CsHudTheme.cs:405-545` ＋ `UI/InGame/ConsolePanel.cs:81`（**三套**）。

| # | 新增 | 能力 |
|---|---|---|
| E-core-16 | `Runtime/Presentation/UIWidgetControls.cs` | `UIFactory` 的通用控件工厂：布局助手 `Place` / `AnchoredTopLeft` / `AnchoredBottom` / `CreateLabel` / `CreateBoxRect` / `CreateBottomLabel`；进度条比例 `SetBarWidth`（**锚点宽度**口径）；控件 `CreateSlider` / `CreateInputField` / `CreateSelector`（「◀ 值 ▶」）/ `CreateToggleRow`；句柄类型 `Selector` / `ToggleRow`；外观结构体 `Widget*Style`。**配色 / 文案 / 字号 / 回调一律参数传入**，引擎不含任何项目取值 |

### 缺陷本质（能力缺口）

引擎 `UIFactory`（E2 起对业务公开）只有 `DefaultFont` / `CreateNode` / `Stretch` / `CreateCentered` / `CreatePanel` / `CreateText` / `CreateButton` / `UICamera` —— 搭骨架的全套脚手架有了，**缺"控件"那一层**。

于是每个项目都自己再搭一遍：同一个 `Slider` / `InputField` 在 `clover-project-cs16` 里被造了**三遍**，三份还**互指对方有坑**（`CsHudTheme.cs:473` 曾写"`CsUiStyle.CreateInputField` 编译不过"—— 实测那是**过时注释**：该方法一直编得过，因为正解 `input.placeholder as Text` 早就在里面）。

### 三个已封进引擎的实测坑（注释随代码一起搬，⛔ 不许丢）

1. **进度条别用空 sprite 的 `Image.fillAmount`** —— 会**静默失效**；正解是 `SetBarWidth` 走**锚点宽度**（与 `WorldHpBar` 同一口径）。
2. **`InputField.placeholder` 的声明类型是 `Graphic`**，直接取 `.font` / `.fontSize` / `.text` ⇒ **CS1061**；正解 `as Text`。
3. **别用 `DefaultControls.CreateSlider`** —— 配色藏在私有层级里，改不干净。

### 视觉零改动（本片只换归属）

搬移前后各抽 30 个 `new Vector2(...)` 数值流做多重集比较 ⇒ **完全一致**（`DIFF: none`）；全部 `fontSize` 由字面量改为 `Widget*Style` 参数，**数字不变**（22 / 24 / 20 / 18 / 26 逐项对照）。

### 判据

- 同名类型只剩一份：`class Selector` = 1、`class ToggleRow` = 1（都在引擎 `Presentation`）；
- 项目三文件的「自建控件构造」残留 = **0**；
- **刻意不新建**：`CsUiStyle.StretchRoot` / `CreateFullScreen` ≡ 引擎 `Stretch` / `CreatePanel` ⇒ 合并（⛔ 不留两份）；
- 编译：引擎 `Presentation` 19 → 20 files，`RESULT: ALL PASS` / `GATE_EXITCODE=0`；项目侧 Roslyn 按 `Cs16.asmdef` 边界 `EXITCODE=0`（含 `/define:UNITY_EDITOR` 变体）。

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/Presentation/UIWidgetControls.cs` 顶部块注释 | ✅ 本片（出处 / 未下沉什么 = 配色与文案 / 为什么 / 语义约束） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` §3.2 表现域 UI 行 | ✅ 本次同步（新增「控件工厂」行 + 适配行补边界） |
| `结构规则.md` §3.5「允许放入 · 界面」/ §4.4 必查能力清单 | ✅ 本次同步 |
| skill（`reference/engine-mental-model.md` §4 表现域能力边界表） | ✅ 本次同步（新增 `UIFactory` 通用控件工厂行） |

### ⚠️ 未决（需用户开一次 Unity）

`Selector` / `ToggleRow` 从 `Cs16.UI` 程序集移到了 `CloverEngine.Presentation`。`Resources/UI/{NewGamePanel,OptionsPanel}.prefab` 里它们是**内联可序列化字段**（YAML 只落字段名 `Value`/`Prev`/`Next`、`Button`/`Value`，字段名逐字未变 ⇒ 按名匹配应能还原）。若打开后出现"缺少选择行引用"的 Warn，跑一次 `Clover/CS16/生成流程场景与面板`（`FlowSetup`，预制体的唯一来源，禁止手写 `.prefab`）重建即可。

---

## E-core-18 · 第一人称相机侧的"通用纯件"只能写在业务工程里 ⇒ `ViewBob` / `CameraMath` / `LookAccumulator` 下沉进引擎（2026-09-19）

**日期**：2026-09-19。**新增文件**：`Runtime/Presentation/ViewBob.cs`、`Runtime/Presentation/CameraMath.cs`、`Runtime/Presentation/LookAccumulator.cs`（各带 `.meta`；**纯新增**，引擎既有文件一行未改）。**来源项目**：`clover-project-cs16`（同轮已完成切换）。

> **性质**：能力下沉（不是修 bug）。
> **编号**：本条占用 `E-core-18`（沿用 `E-core-NN` 连号）。写下当时 `E-core-17` 在本文件未被占用，本片取 `E-core-18` **避开**并行片（agent-27 音效闸门下沉）；该并行片随后落盘时确实用了 `E-core-17`（见本文件该节）⇒ **两片编号无冲突**。

| # | 新增 | 能力 |
|---|---|---|
| E-core-18 | `Runtime/Presentation/ViewBob.cs` | 第一人称**视点晃动 + 落地沉降**：`ViewBobConfig`（7 个数值：满幅基准速度 / 幅度 / 频率系数 / 振幅平滑速度 / 落地沉降幅度 + 时间常数 / 横滚角）+ `ViewBob`（**纯逻辑类，刻意不是 `MonoBehaviour`**）：`Config` / `Offset`（摄像机局部空间偏移）/ `Roll` / `Reset()` / `Tick(dt, speedXZ, onGround)`。**只输出偏移、绝不碰相机**（bob 不影响射线起点，否则子弹随脚步摆） |
| E-core-18 | `Runtime/Presentation/CameraMath.cs` | 三个纯函数：`FovYFromFovX(fovXDegrees, aspect)`（原版 `CalcFov` 等价式；越界回退 90、`aspect<=0` 原样返回）/ `AimDirection(yawDegrees, pitchDegrees)`（`(cos·sin, sin, cos·cos)`；yaw 0 = +Z、pitch + = 抬头；**不做夹紧** —— 限位是玩法口径，留在调用点）/ `Follow(current, target, dt, tau)`（`k = 1-exp(-dt/tau)` 后 `Lerp`，`tau<=0` 直接吸附） |
| E-core-18 | `Runtime/Presentation/LookAccumulator.cs` | 鼠标位移 → yaw/pitch 累加器（**纯逻辑类**）：`Yaw` / `Pitch` / `Reset(yaw, pitch)`（只做 yaw 的 360° 规范化）/ `Add(mouseDeltaX, mouseDeltaY, degreesPerCount, invertY, pitchLimitDegrees)`（水平 `Repeat(…,360)` 不夹紧、垂直夹 `±limit`、Y 反转取负、**不乘 dt**） |

### 缺陷本质（能力缺口）

以第一人称玩法为例，这三件事**每个项目都会重写一遍**，而它们恰恰不含任何玩法决策：

1. **视点晃动的相位 / 振幅 / 落地沉降数学** —— 原项目 `ViewBob.cs`（77 行）**零业务类型引用**，只因为"数值是业务的"就整份挂在业务程序集里；
2. **相机侧的三条纯数学**（FOV 换算 / yaw,pitch → 视线方向 / 指数平滑跟随）—— 它们被塞在一个 774 行的第一人称相机组件里当 `static` 方法，别的项目连"照抄"都得先读懂那个文件；
3. **鼠标位移 → 视角的累加口径**（符号方向、"每 count 多少度"、是否夹紧、是否乘 `dt`）—— 各写各的 ⇒ 手感口径在每个项目里被重新发明一次。

**下沉的不是数值，是形状**：三个件都不含任何项目取值 —— `ViewBobConfig` 由业务传满、`degreesPerCount` 与 `pitchLimitDegrees` 由调用方算好传入、`Follow` 的 `tau` 由调用方选。

### 与既有能力的关系（⛔ 不是第二套）

- 引擎 `Game.Camera`（`CameraManager`）是 **2D / 俯视**语义的相机管理器（`Follow` 锁 Z、`SetBounds` 按正交视口钳位），第一人称机位用不上它；`CloverThirdPersonCamera` 是第三人称环绕机位。本条三个件是**第一人称机位侧的数学 / 状态件**，与上面两者都不重叠，且**同样不经 `Game.Camera` 门面**（业务自持 / 自传配置）。
- `FollowRecoil` **合并**（`结构规则.md` §4.4「已有能力不准再起第二套」）：它只是"按 `|target| > |current|` 在上升 / 回落两个 tau 里挑一个再调 `Follow`"，挑常数用的是业务手感值 ⇒ 引擎**只留一条 `Follow`**，那一步选择内联在调用点（⛔ 不留第二份跟随实现）。

### 自证（离线编译 + 离线断言；证据口径：**未跑 Unity 实机**）

| 项 | 命令 / 结果 |
|---|---|
| 引擎闸门 | `compile-check-client.ps1` ⇒ `RESULT: ALL PASS` / `GATE_EXITCODE=0`（`Presentation` 20 → 23 files） |
| 下游编译（按 asmdef 边界拼 Roslyn） | `clover-project-cs16/client/Assets/Scripts/**`（`Cs16.asmdef` 边界，77 个 `.cs`）对**闸门刚产出的** `csc-out\CloverEngine.*.dll` 编译 ⇒ 生产档 `OK: Cs16 (exit 0)`、编辑器档 `OK: Cs16.Editor (exit 0)`，`RESULT: ALL PASS` / `EXITCODE=0`（0 条 error） |
| 离线断言（真跑：三个件**直链真实源码** + 只补 `Vector3`/`Mathf` 两个替身） | **45/45 ALL PASS**。六组**逐位对拍**全部 0 差异：`ViewBob` 382 帧（`Offset`/`Roll`）、`FovYFromFovX` 96 组、`AimDirection` 210 组（含越界 pitch）、`Follow` 1600 组、合并口径（内联选 tau）240 组、`LookAccumulator` 150 次比较（5 档灵敏度 × 反转开/关 × 14 步位移）。语义断言：静止 ⇒ `Offset` 恰为零；起步首帧偏移 = `Amount×dt×BlendSpeed`（非满幅跳变）；停步后仍带明显偏移继续晃（实测第 16 帧 \|x\| 仍 > 0.005）⇒ 起停共用同一平滑速度；落地只在 `onGround` 假→真那一帧给一次沉降且同帧起走指数衰减、残量 < 0.0005 归零、持续在地面 400 帧不再触发；`Roll` 与 `Offset.x` 同号且 \|`Roll/x`\| = `RollDegrees/Amount`；`FovYFromFovX(90, 16/9) = 58.7155`。 |

### ⛔ 未下沉什么（防越界；含本片对"整组件下沉"的评估结论）

- **不下沉整个第一人称相机组件**：它的位姿不是"喂一个干净的位姿结构体"就能接住的 —— 要 `ICsMatch` 的观战 / 开镜状态、`CsActor` 的眼高与权威后坐力、`CsCombatTuning` 的一串手感值、`CsHitboxProxy` 的自身模型隐藏、自建 `Camera` + `AudioListener` 与"关掉别人的主相机"，还有**与业务射线交错的调用顺序**（算位姿 → 打射线 → 写 Transform）。硬做成引擎组件，它会变成"配置结构体里塞满十几个业务数值 + 要求业务实现一串回调"的东西 —— 那不是干净接口，只是搬了个壳（且会留下一个没人用的空壳组件）。
  **判定数据（本片实跑）**：按字面口径"不引用任何 `Cs*` 类型"的行占比 = **731 / 774 = 94.44%**（≥60% 这一条满足），但另一个条件是"**且**能用一个干净接口承接位姿" —— 它**不成立**（两个条件是与关系）⇒ 只下沉三个纯件。
- 不下沉**相机生命周期**（自建相机 / 音频监听 / 关掉别人的主相机 / 比赛外禁用）：属业务的舞台装配约定。
- 不下沉**手感数值**：7 个 bob 数值、`PitchLimit`、灵敏度换算、后坐力上升 / 回落 tau、开镜过渡 tau、视高平滑 tau 全部留在业务真源。

### 已知边界 / 精度限制（防当万能药用）

- `ViewBob.Config` **必须由业务填满**（`FullAmplitudeSpeed > 0`）：它按比例参与运算，引擎**不做兜底、不替业务定数值**。
- `LookAccumulator` **不读设置、不乘 `dt`**：`degreesPerCount` 与 `pitchLimitDegrees` 由调用方算好传入；`Reset` **只规范化 yaw**，pitch 的夹紧属限位口径（项目侧 `ForceLook` 保留原 `Mathf.Clamp(pitch, ±PitchLimit)`）。
- `AimDirection` **不做俯仰夹紧**：调用点本来就必须先夹（相机的最终俯仰还要再叠受击抖动并再夹一次），引擎不替业务定限位值。
- 三个件都**非线程安全**（主线程使用），且**不依赖任何 `Core` 契约**（只用 `UnityEngine` 的 `Mathf` / `Vector3`）。

### 首个消费方（同轮切换完成）

`clover-project-cs16`：删 `Module/CameraRig/ViewBob.cs`（含 `.meta`）；`FirstPersonCamera` 的 `FovYFromFovX` / `ComputeAimDirection` / `Follow` / `FollowRecoil` 改调 `CameraMath`（⛔ 不留同名 `private static` 副本 —— 全项目搜这三个名字，本地定义命中 **0**）；`PlayerMotor` 的 `_yaw` / `_pitch` 改由 `LookAccumulator` 持有（公开的 `Yaw` / `Pitch` 值一字未变）；`PlayerModule` 用 `ViewBobConfig` 把 7 个原常量传满后 `new ViewBob`。**手感 / 数值零改动**（六组逐位对拍 0 差异）。

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/Presentation/{ViewBob,CameraMath,LookAccumulator}.cs` 源码注释 | ✅ 本片（出处 / 未下沉什么 / 为什么 / 语义逐条 / 已知边界） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` §3.2 Camera 行 | ✅ 本次同步 |
| `结构规则.md` §3.5 表现域「允许放入 · 相机」 | ✅ 本次同步 |
| skill（[`clover-ai-skill/reference/engine-mental-model.md`](https://github.com/qw576483/clover-tools/blob/main/ai-skill/reference/engine-mental-model.md) §4 能力表 `Game.Camera` 行） | ✅ 仓库源副本已追加。⚠️ **宿主安装副本未改**（遵守「执行者不许改 skill 安装副本」）⇒ 两份副本暂时不一致，请主 agent 跑 `scripts/skill-health.ps1` 同步 |

### ⚠️ 未决（需用户开一次 Unity）

本轮**未跑编辑器 / PlayMode**（用户未开 Unity）。三个件是纯逻辑类，离线断言已覆盖其语义；但**接入后的实机观感**（视点晃动 / 落地沉降 / 转视角手感 / 开镜过渡）需要用户开一次 Unity 走一遍第一人称流程确认 —— 本片结论是"数值与调用顺序逐字保留 + 六组逐位对拍 0 差异"，预期无变化。

---

## E-core-17 · 音效只有「每次调用都报缺失」且**没有任何起播闸门** ⇒ 每个项目都自己搭一套（2026-09-19）

**日期**：2026-09-19（**2026-09-20 二轮补正**，见本节末）。**改动文件**：`Runtime/Presentation/Sound.cs`（**四处**缺失分支改 `WarnOnce` + 新增两个闸门与判据）、`Runtime/Core/PresentationContracts.cs`（`ISoundManager` 新增两个属性 —— **纯新增**，既有成员一字未改）。

> **性质**：能力下沉（不是修 bug）。
> **编号**：本条占用 `E-core-17`（`E-core-16` 之后的第一个空号）。
> **来源项目**：`clover-project-cs16` 的 `client/Assets/Scripts/Module/Audio/SfxService.cs`（201 行，本次降为薄转发）
>   ＋ `client/Assets/Scripts/Module/Combat/CombatAudio.cs`（97 行，更早的同款）。

| # | 新增 | 能力 |
|---|---|---|
| E-core-17 | `Runtime/Presentation/Sound.cs` | ① **缺失只报一次**：**四处** `clip == null`（`PlayBGM` `:271` / `PlaySFX` `:335` / `PlaySFXAt` `:360` / `PlayVoice` `:386`）改走 `LogThrottle.WarnOnce("Sound", "missing:" + path, …)`，**文案与 `path` 变量逐字保留**；② 两个可配闸门 `MaxPlaysPerFrame`（单帧最多**真正起播**几次，`:192`）/ `MaxConcurrentPerClip`（同一路径**同时播放**的音源数上限，`:195`）—— **默认都是 `0` = 不限**；③ 判据 `AllowPlay`（`:208`）+ `CountPlaying`（`:247`）：超限**丢弃该次播放**（⛔ 不排队、⛔ 不打断正在播的音源）+ **归还本次加载的资源引用** + `LogThrottle.WarnThrottled` 限频告警（帧口径 key = `playcap.frame`、并发口径 key = `playcap.clip:<path>`） |
| E-core-17 | `Runtime/Core/PresentationContracts.cs`（`:354` / `:364`） | `ISoundManager` 新增 `int MaxPlaysPerFrame { get; set; }` / `int MaxConcurrentPerClip { get; set; }`。**为什么必须挂在契约上**：实现类 `SoundManager` 是 `internal`（G1），业务只拿得到 `Game.Sound`（接口）—— 挂在实现类上等于"业务配不了"，闸门就永远是默认值（= 没有闸门）。取值来源仍是**业务**（cs16 的 `CsAudioTuning.MaxPlaysPerFrame` / `MaxConcurrentPerClip`，由 `AudioModule.Start` 下发），与 `LogThrottle` 的 `everyN` 由业务传入是同一分工 |

### 缺陷本质（能力缺口）

`Sound.cs` 在本次下沉前有三处短板，**每一处都被"每个新项目自己再写一遍"验证过**：

1. **缺失"每次调用都报"**：`PlayBGM` / `PlaySFX` / `PlaySFXAt` / `PlayVoice` 四处在 `clip == null` 时裸 `Game.Logger?.Warn("Sound", …)`。
   脚步 / 命中 / 蜂鸣是**每秒多次**的高频路径 —— 只要某个音效没落地，日志就被同一条 Warn 刷爆，真问题反而看不见。
   （cs16 的 `SfxService.cs` 与 `CombatAudio.cs` 顶部注释都为此写了"为什么不能直接 `Game.Sound.PlaySFX(name)`"。）
2. **全类没有任何起播闸门**：一帧里打进几十个音效（AOE / 群体战 / 多人脚步）会把音源池占满，之后所有音效走
   `GetAvailableSource` 的池满分支被丢弃 —— 表现为"枪声/脚步互相顶掉"。`E-build-02` 把池从 8 提到 32 只是**把池加大**，
   不是**限流**：需求本身（"同一音效同时别响超过 N 个"）仍然没有落点。
3. **缺失没有"只告警一次"的口径** ⇒ 项目侧只能自建**探测状态表**（`SfxService` 的 `ClipState` 表 / `CombatAudio` 同款），
   两份实现连阈值与文案都不一致。而它们要挡的其实是**同一个**资源层现象：`ResourceManager.CompletePending` 对
   加载失败是"**每次调用**一条 `Error("Resource", "加载失败：{path}")`"。

**为什么下沉**（`结构规则.md` §4.4「已有能力不够用时**优先扩展原实现**，⛔ 不准平行再起一套」）：
闸门是任何项目都要的底座（高频缺失要防刷屏、一帧多音效要限流）。cs16 已经抄了两份；再抄下去就是"每个新项目重写一次 8 行计数逻辑、口径还各不相同"。

### 语义约束（照 `LogThrottle.cs:1-26` 的版式写进文件头 + XML 注释；改一条 = 语义漂移）

1. **默认 `0` = 不限 = 与下沉前逐字一致**：不设两个闸门时，播放路径的行为（起播次数 / 日志 / 资源引用计数 / 音源池取源顺序）**一个字节都没变**。
2. 两个闸门**只管 SFX / Voice 的起播**（`PlaySFX` / `PlaySFXAt` / `PlayVoice`）：**BGM / 分组音量 / 淡入淡出 / `TakeSource` 的池逻辑 / `Dispose` 语义一律不动**。
3. 超限 ⇒ **丢弃该次播放**（⛔ 不排队、⛔ 不打断正在播的音源），并**限频告警**（`LogThrottle.WarnThrottled`，⛔ 不许裸 `Warn`）。
4. `MaxConcurrentPerClip` 的口径 = **同一路径当前正在播的音源数**（**真并发**，不是"时间窗内起播了几次"）；
   路径含分组前缀（`Sound/SFX/…` / `Sound/Voice/…`）⇒ 不同分组互不干扰，BGM 不参与。
5. 缺失（`clip == null`）**整进程只报一次 / 路径**（`LogThrottle.WarnOnce`，key = `missing:<path>`）。
6. **未下沉**（那些是业务）：音效名表与路径命名口径、分组音量值、`cs.*` 设置键与音量应用策略、`Prewarm` 的列表；
   项目侧"探测一次、Ready 才播"的缓存也**不需要** —— 引擎 `Sound` 的加载本身就是异步 + 缓存，缺失由 `WarnOnce` 与
   资源层负责；要"问一句这条在不在"用 `Game.Res.Exists`（E-core-09，按路径缓存，不记账）。

### 判据 / 自证（本片口径：**离线编译 + 离线断言**；用户没开 Unity，**未跑**编辑器 / PlayMode）

| 项 | 依据（可复跑） |
|---|---|
| `Sound.cs` 裸 Warn **归零（file-wide 0 命中）** | `Select-String Sound.cs -Pattern 'Game\.Logger\?\.Warn\("Sound"'` ⇒ **0 命中**：① 四处 `clip == null`（`PlayBGM` / `PlaySFX` / `PlaySFXAt` / `PlayVoice`）全走 `LogThrottle.WarnOnce("Sound", "missing:<path>", …)`；② `GetAvailableSource` 的池满告警走 `LogThrottle.WarnOnce("Sound", "pool.exhausted", …)`（⛔ 池逻辑与 `_poolExhaustedWarned` **原样保留**，只换发射通道）。见下方「二轮补正」——首轮曾留 2 处裸 Warn |
| 默认值零行为变化 | 离线断言 A：默认 `0` 时同帧 20 次 `PlaySFX` **全部起播**、全程 **0 条日志**；同 clip 连续 3 次也不丢（无并发闸门） |
| 单帧上限 | 离线断言 B：`MaxPlaysPerFrame = 2` ⇒ 前 2 次起播、第 3/4 次被丢弃，**只 1 条告警**；时钟跨过限频窗口后再丢会**再出一条**（是"限频"不是"只报一次"）；换帧后计数复位；丢弃时**各归还一次**资源引用 |
| 并发上限 | 离线断言 C：`MaxConcurrentPerClip = 2` ⇒ 同 clip 第 3 次被丢弃 + 1 条告警；**另一条 clip 不受影响**；音源播完后同 clip 又能起播（证明是"当前正在播的音源数"口径） |
| 缺失只报一次 | 离线断言 D：同一路径缺失 3 次**只 1 条** Warn 且文案逐字保留；不同路径各自 1 条；`PlaySFXAt` / `PlayVoice` 分支同款 |
| BGM 不受闸门影响 | 离线断言 B6：单帧名额用尽后 `PlayBGM(..., 0f)` **仍起播** |
| 引擎编译闸门 | `.codebuddy/doc-audit/tools/compile-check-client.ps1` ⇒ `RESULT: ALL PASS` / `GATE_EXITCODE=0`（Core 27 files、Presentation 23 files） |
| 项目侧编译 | 按 `Cs16.asmdef` 边界 Roslyn（参考集 = 闸门**刚产出的** `csc-out\CloverEngine.*.dll`，⛔ 不用 `Library\ScriptAssemblies\` 的旧 dll）⇒ `RESULT: OK: Cs16` / `EXITCODE=0`（首轮 77 files；复派轮 78 files，`ERRORS=0`） |
| 断言宿主 | 临时宿主 `.ai-tmp/test/soundgate/`：**链接真实** `Sound.cs` + `LogThrottle.cs`（非复制品；`ISoundManager` / `SoundGroup` / `ILogger` / `IResourceManager` / `IEventBus` 用**子集镜像** —— 真契约由引擎闸门编真文件覆盖），Unity 替身只补外部符号；`LogThrottle.Clock` 注入假时钟 ⇒ 限频确定可复现。**用完即删**（复派轮重建后跑出 30 PASS / 0 FAIL，`ALL PASS`；`dotnet run` 退出码 0） |

### 二轮补正（2026-09-20 · agent-27 复派核验）

首轮落地时 `Sound.cs` 里仍留 **2 处**裸 `Game.Logger?.Warn("Sound", …)`（首轮按"契约只点名三处 + BGM/池逻辑一律不动"刻意未动）。
复派轮按接受判据「`Sound.cs` 里裸调归零」（**file-wide**，不是三处）一并归零 —— **只换日志发射通道**：

| # | 落点 | 改法 | 受保护行为 |
|---|---|---|---|
| E-core-17（补） | `PlayBGM` 的 `clip == null` 分支 | 裸 Warn ⇒ `LogThrottle.WarnOnce("Sound", "missing:" + path, $"BGM 加载失败（clip 为空）：{path}")`。**为什么必须补**：文件头语义约束 ⑤ 写的是"**所有** `clip == null` 整进程只报一次/路径" ⇒ 首轮漏了 BGM 这条，代码与**自己的注释**不一致 | BGM 的播放 / 淡入淡出 / 请求序号判定**一行未动**（仅告警频率：每次 ⇒ 每路径一次） |
| E-core-17（补） | `GetAvailableSource` 的池满告警 | 裸 Warn ⇒ `LogThrottle.WarnOnce("Sound", "pool.exhausted", …)`，文案逐字保留 | ⛔ 池逻辑（取源失败 `return null`）与 `_poolExhaustedWarned` **原样保留** ⇒ "只报一次"的原语义逐字不变 |

补的断言（同一宿主，复派轮 **30 PASS / 0 FAIL**，`ALL PASS`）：`D4` `PlayBGM` 同路径 3 次只 1 条；`E3/E4` 池满告警 1 条、再触发仍是 1 条；`A2/F1` 默认 `0` / 负值下仍 0 条日志。
文件头块注释同步：出处 ① 补 `PlayBGM`、新增 ④（日志通道不统一）与语义约束 ⑥（本文件无裸 `Warn`）。

### 已知边界（防当万能药用）

- `MaxConcurrentPerClip` 是**真并发**口径（同一路径当前正在播的音源数），**不是**项目侧原先的"0.10s 滑动窗口内起播次数"
  （cs16 的 `CsAudioTuning.ClipWindow` 在下沉后的链路里**不再被读取**；该常量留在原位未删 —— `CsAudioTuning.cs` 不在本片允许改动的文件里）。
- 单帧计数发生在**真正起播**的那一帧（异步加载晚到的音效算在它**响**的那一帧），不是"被请求"的那一帧。
- **丢弃 ≠ 排队**：被丢弃的那次播放不会在池空出来时补播（排队会让玩家听到一串迟到的声音）。
- 限频告警的 key 固定不含帧号（`playcap.frame`）：否则限频表的 key 会随帧号无限增长，且每帧都是"新 key" ⇒ 等于没限频。
- 引擎资源层对**每次加载失败**打一条 `Error("Resource", "加载失败：{path}")`（`ResourceManager.CompletePending`）—— 本片**未动**
  （不在允许改动范围）。它正是项目侧探测缓存当年要挡的第二条喷发源；下沉后由业务侧"先 `Game.Res.Exists` 再转发"承担。

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/**` 源码注释 | ✅ `Sound.cs` 顶部块注释（出处 / 未下沉什么 / 为什么 / 语义约束 ①~⑤）+ `ISoundManager` 两个属性的 XML 注释（口径 + "默认值不许改"的硬约束） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` §3.2 Sound 段 | ✅ 本次同步（新增「播放闸门（E-core-17）」行；「设置持久化 **未实现**」那条边界**原样保留**） |
| `结构规则.md` §4.4 必查能力清单 | ✅ 本次同步（该清单原本**没有** `Sound` 条目 ⇒ 新增一条并写明"⛔ 别在业务侧再搭一套"） |
| skill（[`clover-ai-skill/reference/engine-mental-model.md`](https://github.com/qw576483/clover-tools/blob/main/ai-skill/reference/engine-mental-model.md) §4 能力表 `Game.Sound` 行） | ✅ 本次同步（追加两个闸门属性 + E-core-17）。⚠️ 只改了**仓库副本**；宿主安装副本 `~/.codebuddy/skills/ai-skill/` 按执行纪律未动，两份同步留给主 agent |
| 项目侧（`clover-project-cs16`） | ✅ `Module/Audio/SfxService.cs` 由"音效闸门"降为**薄转发**（探测状态表 / 帧计数 / 并发窗口 / 缺失计数四套记账全部删除）+ `Module/Audio/AudioModule.cs` 下发阈值（**调用点唯一改动处**：新增 `ApplySfxGates()` 与它在 `Start` 里的一次调用） |

⚠️ **未决**：cs16 的 `Module/Combat/CombatAudio.cs` 仍留着自己那份探测缓存（本片契约只点名 `SfxService`，且不许改该文件）
⇒ 同一个模式还有一处未收敛。

---

## E-core-19 · `IsoLayout` 的「格增量 → 8 方向」表整体逆时针偏 45° ⇒ 人物/怪物朝向"看着不对"（2026-09-19）

**日期**：2026-09-19。**文件**：`Runtime/Core/IsoLayout.cs`（`DirectionTo` / `DirectionDelta` 两个方法 + 它们的 XML 注释；**公开签名一个都没动**）。

> **编号**：本条占用 `E-core-19`（沿用 `E-core-NN` 连号；`E-core-18` 之后的下一个空号）。
> **性质**：**修 bug**（不是能力下沉）—— 是对 `E-core-08` 引入的 `IsoLayout` 的方向表订正。
> **来源项目**：`clover-project-diablo2`（用户原话「3.人物模型朝向好像都不对」）。

| # | 改动 | 说明 |
|---|---|---|
| E-core-19 | `IsoLayout.DirectionTo(Vector2Int)` 的 8 个分支整表订正 | 旧表整体逆时针偏一档（45°）；新表见下 |
| E-core-19 | `IsoLayout.DirectionDelta(Dir8)` 的 8 个分支整表订正（逆表） | 与 `DirectionTo` **必须整表一起改**（只改一边会让 `DirectionTo(DirectionDelta(d)) != d` 且不报错） |
| E-core-19 | 两个方法的 XML 注释 | 旧注释里那张"`(0,+1) → S`"的表本身就是错的（照着错的注释写成重的代码）⇒ 换成由 `GridToWorld` 反算的权威表 + 推导 + 自洽判据 |

### 缺陷本质

`IsoLayout.GridToWorld` 是 `x = (gx − gy)·HalfW`、`y = −(gx + gy + 1)·HalfH`
⇒ 格增量 `(Δgx, Δgy)` 对应的世界位移是 `( (Δgx − Δgy)·HalfW , −(Δgx + Δgy)·HalfH )`。
把这个代入旧表，**每条都差一档**：

| 格增量 | 世界位移（HalfW=1, HalfH=0.5） | 真实屏幕方向 | 旧表返回 |
|---|---|---|---|
| `(+1, +1)` | `(0, −1)` | **S**（正下） | `SE` ✗ |
| `(0, +1)` | `(−1, −0.5)` | **SW**（左下） | `S` ✗ |
| `(−1, +1)` | `(−2, 0)` | **W**（正左） | `SW` ✗ |
| `(−1, 0)` | `(−1, +0.5)` | **NW**（左上） | `W` ✗ |
| `(−1, −1)` | `(0, +1)` | **N**（正上） | `NW` ✗ |
| `(0, −1)` | `(+1, +0.5)` | **NE**（右上） | `N` ✗ |
| `(+1, −1)` | `(+2, 0)` | **E**（正右） | `NE` ✗ |
| `(+1, 0)` | `(+1, −0.5)` | **SE**（右下） | `E` ✗ |

即：**旧表把"世界位移真正指向的屏幕方向"整体逆时针偏了一档**。

**为什么这是"静默"缺陷**：旧表与旧逆表**互相自洽**（`DirectionDelta(DirectionTo(d)) == d` 在旧代码上是成立的）
⇒ 任何"往返一致"式的自检都发现不了它（见下方最小复现的 ③ 段：OLD 在 ③ 上**全过**）。
它只在"与**几何**对照"时才暴露。叠加到业务上就是：格增量算出的朝向与素材（`.dcc` 的 8 向图）
所指的朝向差 45° ⇒ 用户看到的"人物朝向不对"。

### 正确表（权威）

```
(0,+1) → SW    (0,-1) → NE    (+1,0) → SE    (-1,0) → NW
(+1,+1) → S    (-1,-1) → N    (+1,-1) → E    (-1,+1) → W
```
出处两条（互相印证）：① 由上面 `GridToWorld` 的公式直接反算；
② Diablo2 参考实现 Diablerie `Engine/Iso.cs:103-109` 的 `Direction(pos, target, directionCount)`
   （基准向量 `(1,1)` = 世界 `(0,−1)` = 屏幕正下，顺时针增角）—— 它的 8 向序号按
   `map → 世界` 投影后同样是「`(+1,+1)` 正下、`(0,+1)` 左下」。

### 最小复现（修复前失败 / 修复后通过）

宿主：`clover-project-diablo2/.ai-tmp/hosts/dircheck/`（**这份宿主刻意保留**，是本条的最小复现；
**链接真实源码** `Runtime/Core/{IsoLayout,Dir8,LogThrottle}.cs`；
只补 `Game.Logger` 与 `UnityEngine.Time` 两个替身；另有一份**逐字复制**的 `IsoLayoutOld` 作对照）。

```bash
cd clover-project-diablo2/.ai-tmp/hosts/dircheck
dotnet run -c Release
```

实际输出（33 条断言 × 2 套实现）：

```
############ OLD（修复前） ############
--- ① DirectionTo：8 个格增量 → 期望 Dir8 ---
  [FAIL] ( 1, 1) → S   (got SE)      [FAIL] ( 0, 1) → SW   (got S)
  [FAIL] (-1, 1) → W   (got SW)      [FAIL] (-1, 0) → NW   (got W)
  [FAIL] (-1,-1) → N   (got NW)      [FAIL] ( 0,-1) → NE   (got N)
  [FAIL] ( 1,-1) → E   (got NE)      [FAIL] ( 1, 0) → SE   (got E)
--- ② 独立推导：GridToWorld → WorldToGridContinuous → Diablerie 角度公式 ---
  [FAIL] 位移 ( 1, 1) ⇒ map(1,1) ⇒ 角度公式给 S / DirectionTo 给 SE
  [FAIL] 位移 ( 0, 1) ⇒ map(0,1) ⇒ 角度公式给 SW / DirectionTo 给 S
  …（8 条全 FAIL，全部"角度公式"比 DirectionTo 大/小一档）
--- ③ 往返自洽：DirectionDelta(DirectionTo(delta)) 与 delta 同号 ---
  [ OK ] ×8   ← ⚠️ 旧表在 ③ 上**全过**：它自洽，只是与几何错开一档
--- ④ 回归护栏（旧错表必须不再成立）---
  [FAIL] (0,+1) 不再返回 S   (got S)     [FAIL] (+1,+1) 不再返回 SE   (got SE)
  [FAIL] (+1,0) 不再返回 E   (got E)     [FAIL] (+1,-1) 不再返回 NE   (got NE)
  [FAIL] (0,-1) 不再返回 N   (got N)
--- ⑤ 只看符号（(3,7) 与 (1,1) 同解）---
  [FAIL] DirectionTo(3,7) == S   (SE)    [FAIL] DirectionTo(0,5) == SW   (S)
  [FAIL] DirectionTo(-9,0) == NW   (W)
--- OLD（修复前）：失败 24 / 33 ---

############ NEW（修复后 = 引擎真实源码） ############
--- ① …8 条全 [ OK ]  --- ② …8 条全 [ OK ]（角度公式与 DirectionTo 逐条一致）
--- ③ …8 条全 [ OK ]  --- ④ …5 条全 [ OK ]  --- ⑤ …3 条全 [ OK ]
--- NEW（修复后）：失败 0 / 33 ---

===== 汇总 =====
  OLD 失败项 = 24 / 33
  NEW 失败项 = 0 / 33
  判据：OLD 必须 > 0（修复前确实错）、NEW 必须 == 0
===== DIRCHECK: ALL PASS =====
```

> ①②③ 三层判据是**互相独立**的：① 是"权威表"；② 只用到 `GridToWorld`/`WorldToGridContinuous`
> 与 Diablerie 的角度公式（**不碰 `DirectionTo` 的实现**）⇒ 它才是"独立判据"；
> ③ 是"表与逆表是否互相自洽"（**单看它发现不了本缺陷** —— 这正是旧代码能一路活下来的原因）。
> ⚠️ 判角度必须在 **map 空间**判：屏幕上的等距投影把 map 的 45° 斜向压成 2:1
> （斜向 `Δworld` 只有 26.57°）⇒ 拿"屏幕夹角 45°"当判据会误报（本片第一版这么写过，已订正）。

### 已知边界（防当万能药用）

- 本表只定义「格增量 → **屏幕**朝向」。**素材侧**（`.dcc` 的方向槽位 ↔ 方向名）是**另一处**独立缺陷
  （`clover-project-diablo2/tools/d2codec/export_chars.py` 漏了 `DirectionMapping` 那一层），
  两处都修完朝向才对得上 —— 只修一边会得到"几何对了但素材还是错的"。
- `DirectionTo` 收**零增量**时仍按 `Dir8.S` 返回（并限频告警一次）—— 行为未改（属既有契约）。
- `DirectionDelta` 的未登记 `Dir8` 仍返回 `(0,0)` + 限频告警 —— 行为未改。

### 消费方（改后逐个复核）

| 调用点 | 修完后是否仍然正确 |
|---|---|
| `clover-project-diablo2` `Module/View/ViewModule.cs:215`（NPC 转身面向玩家） | ✅ 更准：以前 NPC 的贴图朝向比真实朝向偏一档（"看着别扭"） |
| `Module/View/ViewModule.cs:1011`（受击反向位移：`Iso.DirectionDelta(v.Dir)` 取反） | ✅ 语义不变（"朝向的反方向"），且现在与素材朝向一致 |
| `Module/View/ViewModule.cs:1120`（调试输出 `期望Dir`） | ✅ 仅日志，修后更准 |
| `Module/Monster/MonsterRuntime.cs:271`（怪物按格变化更新朝向） | ✅ 更准（同 ViewModule） |
| `Module/Player/PlayerMotor.cs:167`（玩家沿路点朝向） | ✅ 更准（同 ViewModule） |
| `.ai-tmp/hosts/combatcheck/Program.cs:897,1282,1315`（离线宿主给玩家摆朝向） | ✅ 仅作"设置朝向"用，无值断言；宿主链接的是**引擎真实源码** ⇒ 自动跟随本修复 |

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/Core/IsoLayout.cs` 源码注释 | ✅ 本片（权威表 + 推导 + 为什么旧表错 + 自洽判据 + E-core-19 交叉引用） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| 项目侧 `clover-project-diablo2/client/Assets/Scripts/Core/Iso.cs`（薄门面的 `<summary>`） | ✅ 本片（同一张表 + 指向引擎权威说明；**只改注释，代码一行未动**） |
| `clover-client-unity-engine-index.md` / `结构规则.md` / `clover-doc/` / skill | ⏳ **未做**（本片契约只允许改 `Runtime/Core/IsoLayout.cs` 与 `修复记录.md` 两个引擎文件；这四处留给主 agent 统一补） |

---

## E-ui-01 · `UIManager` 把画布参考分辨率**写死** 1920×1080 / match=0.5 ⇒ 竖版项目画不出竖画布（2026-09-20）

**日期**：2026-09-20。**文件**：`Runtime/Presentation/CloverPresentation.cs`（**新增 2 个公开静态属性**）、
`Runtime/Presentation/UI.cs`（`UIManager` 构造函数把两行字面量改为读属性 + 末尾 1 条判据日志）。
**既有公开成员一个都没动**（纯新增）。

> **性质**：能力缺口（不是崩溃型 bug）—— 引擎的 UI 适配参数**写在代码里**，业务侧只能改引擎。
> **编号**：本条占用 `E-ui-01`（沿用本文件既有的 `E-<域>-<序号>` 体例，同 `E-net-01` / `E-input-01` / `E-audio-01`；
> `E-ui-` 前缀此前全文未用过，与 `E1`~`E-core-19`、`E-build-*`、`E-editor-01`、`E-res-*` **均不冲突**）。
> **来源项目**：`clover-project-cr` —— 用户 2026-09-20 原话：「**竖版游戏，你用横板ui，真有你的**」。

| # | 改动 | 说明 |
|---|---|---|
| E-ui-01 | `CloverPresentation` 新增 `public static Vector2 ReferenceResolution { get; set; } = new Vector2(1920f, 1080f);` | **默认值 = 旧代码里写死的值** ⇒ 不设置它时行为逐字一致（既有横版项目零影响） |
| E-ui-01 | `CloverPresentation` 新增 `public static float MatchWidthOrHeight { get; set; } = 0.5f;` | 同上，默认 = 旧的硬编码 `0.5` |
| E-ui-01 | `UIManager` 构造：`scaler.referenceResolution = CloverPresentation.ReferenceResolution;` / `scaler.matchWidthOrHeight = CloverPresentation.MatchWidthOrHeight;` | 只把两行字面量换成读属性；`uiScaleMode` / `screenMatchMode` 一行未动 |
| E-ui-01 | `UIManager` 构造末尾新增 `Game.Logger?.Info("UI", "画布适配：参考分辨率=… / match=… / Screen=…")` | 判据日志：业务若写晚了（在 `Game.Launch` 之后）这里仍打印旧的 `1920×1080`，一眼可查 |

### 缺陷本质

`UIManager`（`Runtime/Presentation/UI.cs`）在构造函数里**写死**常驻 Canvas 的适配参数：

```csharp
scaler.referenceResolution = new Vector2(1920f, 1080f);   // 横版
scaler.matchWidthOrHeight  = 0.5f;
```

而 `UIManager` 由 `CloverPresentation.Init`（`Game.Launch` 的挂载钩子）`new` 出来
（`CloverPresentation.cs`：`if (Game.UI == null) Game.AttachUI(new UIManager());`）
⇒ **这是全引擎唯一一次设置画布适配的地方，业务侧没有任何合法入口**。

对竖版（portrait）项目后果是**整屏错**：画布按 1920 宽布局，而竖屏画布宽只有 1080
⇒ 要么被 `CanvasScaler` 缩放糊掉、要么各面板按 1920 宽横排。且它**不报错、不打日志**，
现象只是"UI 看着不像原版" —— 属「数字全对但画面不像」那一类静默失效。

### 最小复现（修复前失败）

竖版项目侧写一行 `CloverPresentation.ReferenceResolution = new Vector2(1080f, 1920f);` → 编译失败：

```
error CS0117: 'CloverPresentation' does not contain a definition for 'ReferenceResolution'
```

（真实形态就是 `clover-project-cr` 被逼着接受横版画布：`client/ProjectSettings/ProjectSettings.asset:11`
`defaultScreenOrientation: 4`（LandscapeRight）、`:44-45` `defaultScreenWidth/Height = 1920/1080`，
而 `client/Assets/Scripts/UI/CrUiStyle.cs` 只能用 `preserveAspect = true` 给竖构图原图**两侧留黑边**兜住。）

### 修复后自证

- **默认值零行为变化（结构判据）**：两个属性默认 `1920×1080` / `0.5` = 旧代码里那两个字面量；
  不设置它们时构造函数读到的值与改前**逐字相同**（改后 `UI.cs` 里已无 `1920f, 1080f` / `0.5f` 字面量）。
- **业务侧生效判据（运行时，`clover-project-cr` 实测）**：进 Play 后日志出现
  `画布适配：参考分辨率=1080×1920 / match=0 / Screen=…`（画布宽 = 1080 = 设计画布宽）；
  竖版画布截图见该项目 `.ai-tmp/screenshots/`。
- **业务侧编译**：`clover-project-cr/client` 经 `Packages/manifest.json` 的 `file:` 本地包引用本引擎
  ⇒ `unity command recompile_status` = `completed / failed=false / errors=[]`。

### 已知边界 / 精度限制（防当万能药用）

- **只在 `UIManager` 构造时读一次**：必须在 `Game.Launch` **之前**设置；Launch 之后再改**不生效**
  （画布已建好）且**不报错** —— 判据就是构造末尾那条日志。
- 只影响**常驻 Canvas 的缩放适配**。`Screen.orientation` 与 `PlayerSettings.defaultInterfaceOrientation`
  是**另一层**（真机屏幕方向），引擎**不代管** —— 由业务代码与工程设置各自负责。
- `matchWidthOrHeight` 取什么值完全由调用方决定：引擎**不替业务选**（横版惯用 0.5，竖版惯用 0）。

### 用法 + 首个消费方

```csharp
// 必须在 Game.Launch 之前（UIManager 在 Launch 的挂载钩子里构造）
CloverPresentation.ReferenceResolution = new Vector2(1080f, 1920f);  // 竖版设计画布
CloverPresentation.MatchWidthOrHeight  = 0f;                          // match width ⇒ 画布宽恒为 1080
Screen.orientation = ScreenOrientation.Portrait;
Game.Launch(config);
```

`clover-project-cr`（首个消费方）：`client/Assets/Scripts/App/Bootstrap.cs` 的 `LaunchEngine()` **第一行**三句
（T1 朝向治理；约定见该项目 `docs/client-architecture.md` §7 与 `UI/CrUiStyle.cs` 的 `DesignW` / `DesignH`）。

### 文档同步

| 落点 | 状态 |
|---|---|
| `Runtime/**` 源码注释 | ✅ 本片（两个新属性的 XML 注释写全「必须在 Launch 之前 / 默认值 = 旧硬编码 / 竖版为什么取 match=0」；`UI.cs` 两处行内注释） |
| 引擎仓库文档（本文件） | ✅ 本节 |
| `clover-client-unity-engine-index.md` / `结构规则.md` / `clover-doc/` / skill | ⏳ **未做**（本片契约只允许改 `Runtime/Presentation/{CloverPresentation,UI}.cs` 与 `修复记录.md`；这四处留给主 agent 统一补） |
| 项目侧（`clover-project-cr`） | ✅ `App/Bootstrap.cs`（LaunchEngine 第一行三句）+ `UI/CrUiStyle.cs`（去掉 `preserveAspect` + 新增 `DesignW`/`DesignH`）+ `docs/client-architecture.md` §7 + `策划/策划案/皇室战争参考规格.md` §0 |
