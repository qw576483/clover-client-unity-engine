# clover-client-unity-engine 结构规则

> **生效范围**：`clover-client-unity-engine` 全仓。
> **目的**：统一客户端引擎的能力域划分、目录归属、依赖方向、命名与契约规则，避免结构失控。
> **用法**：本文不是理念说明，而是**长期有效的执行规则**。以后任何 agent / 人工在新增模块、编写代码、加引用、改 asmdef、做 code review 时，都必须按本文逐条对照。
> **配套**：目录总览、各能力域速查见 [`clover-client-unity-engine-index.md`](clover-client-unity-engine-index.md)。
> **参照**：服务端引擎的对应文档是 [`clover-server-engine/结构规则.md`](https://github.com/qw576483/clover-server-engine/blob/main/结构规则.md) 与 [`clover-server-engine/clover-server-engine-index.md`](https://github.com/qw576483/clover-server-engine/blob/main/clover-server-engine-index.md) —— 两套文档**同构但不同规则**，客户端不镜像服务端分层。

---

## 一、总原则

1. **能力域划分，不镜像服务端分层。** 按客户端真实需要的能力（网络 / 场景 / 资源 / 对象池 / UI / 输入…）划分模块，不按服务器分层。
2. **门面收口。** 业务只从 `Game` 门面拿各模块入口；每个模块对外只暴露 `public interface` + 纯数据 + 工厂，实现类 `internal`。
3. **主线程唯一出口。** 网络 / 资源 IO 在后台线程，所有业务回调经 `Dispatcher` 回 Unity 主线程。
4. **Entity 与 View 解耦。** Entity 是纯数据（不继承 `MonoBehaviour`），View 是 `GameObject`，二者异步绑定。
5. **客户端不跑权威逻辑。** 数值 / 战斗 / 掉落权威在服务端，客户端只做表现、预测、插值、缓存。
6. **资源全池化、全异步。** 战斗内禁止裸 `Instantiate`、禁止同步加载资源。
7. **能力内置、项目选用。** 资源双后端、移动插值、骨骼动画等均为引擎**内置基础能力**，引擎必须提供；具体项目用不用、用哪档，是项目自己的事。
8. **先判断归属，再写代码。** 任何新目录 / 新模块 / 新程序集落地前，先判断它属于哪个能力域。**答不清就不准建。**
9. **不允许把“先放这里以后再说”当成正式方案。** 临时落点若没有明确收口计划，就等于错误落点。

> 「与服务端的对齐」只允许发生在两个层面：
> ① **API 语义**：`OnMsg` / `On` / `Timer.After/Every` / `Fsm.Trigger` 拼写与语义一致；
> ② **网络协议**：帧格式、EMsg 消息号、ObjectID 位布局逐字节一致。
> 除此之外**禁止**镜像服务端目录结构。

---

## 二、统一结构哲学

客户端只允许按下面 **5 个能力域 + 2 个横切**来组织：

```text
Runtime/
  Core/           # 基础域：契约 + 通用底座（不依赖任何其他模块）
  Data/           # 数据域：配表、多语言
  Network/        # 网络域：连接、会话、路由、同步、HTTP
  Resource/       # 资源域：后端抽象、加载、热更
  Presentation/   # 表现域：场景、实体、UI、输入、音频、画质
Editor/           # Editor 横切：仅编辑器可编入，不进玩家包
Tests/            # EditMode + PlayMode 测试
```

### 2.1 依赖方向（asmdef 编译期强制，铁律）

```text
CloverEngine.Core          → []            （基础域，不依赖任何人）
CloverEngine.Data          → [Core]
CloverEngine.Network       → [Core]
CloverEngine.Resource      → [Core]
CloverEngine.Presentation  → [Core]
```

1. **各模块程序集只引用 `Core`，模块之间互不引用。** `Game` 门面在 `Core`，聚合全部。
2. **表现域内部各模块同层**：UI 不依赖 Scene，Sound 不依赖 Animation。同层之间也不允许互相引用程序集。
3. **凡会被 `Core` 接口签名 / 事件 / 属性直接暴露的类型，必须定义在 `Core`。**

**为什么模块之间不能互相引用**：`Core` 的契约（`IAlert.OnAlert` 的事件参数、`IFrameRoom` 的返回类型等）
会直接暴露协议载体类型，而 Unity **不支持程序集循环引用**。一旦在「`Network → Core`」之外再出现
「`Core → Network`」，整个包会因「使用未引用程序集的类型」而编译失败。
因此协议载体（如 `FrameRoomReply`）放在 `Runtime/Core/ProtocolContracts.cs`，与实现分处两个程序集。

### 2.2 新增模块判定五问

新增目录 / 程序集前，必须先回答下面五问之一：

1. 它是不是**跨模块契约或通用底座**（Event / Timer / Fsm / Dispatcher / Logger / 实体池契约…）？→ `Core`
2. 它是不是**配表 / 多语言 / 本地存档**？→ `Data`（本地存档 `Setting` 实现落在 `Core`，见 §3.2）
3. 它是不是**连接 / 协议 / 路由 / 同步 / HTTP**？→ `Network`
4. 它是不是**资源加载 / 热更 / 图集 / 对象池**？→ `Resource` / `Presentation`
5. 它是不是**表现层**（场景 / 实体 / UI / 动画 / 音频 / 输入 / 相机 / 画质）？→ `Presentation`

**如果答不清，就不准建。** 尤其**禁止**新增第六个能力域 —— 新能力优先落进已有域的子模块。

### 2.3 新增目录硬规则

1. **优先在同域内新增文件 / 子模块，不优先新增目录。**
2. 除非形成长期稳定的大类，否则**禁止新增一级能力域**（即禁止新增第 6 个 asmdef）。
3. 新增目录必须同时满足：有明确职责、有明确调用方、有明确「不该放什么」的边界说明。
4. 仅因为「当前方便」而新增目录，一律视为错误设计。
5. 新增 asmdef 必须同步在本文 §2.1 的依赖表里登记，且**只能引用 `Core`**。

---

## 三、各能力域规则

### 3.1 Core（基础域）

**允许放入**

- 跨模块契约与纯数据：`Contracts.cs`、`ProtocolContracts.cs`、`PresentationContracts.cs`、`ResourceContracts.cs`、`TransportContracts.cs`、`ObjectData.cs`
- 通用底座：`Event`（事件总线）、`Timer`（`After` / `Every` / `StopNamed` / `StopScope`）、`Fsm`（**对外可实例化**：`Game.NewFsm()` 每次返回一棵独立状态机，`Tick` 由持有者按帧驱动）、`Separation2D`（角色间**水平**推开：`TryResolve` / `TryResolveOne`，纯函数 / 无状态 / 热路径零分配）、`Dispatcher`、`Logger`、`ConsoleLogger`、`Json`、`JsonWriter`（**确定性 JSON 原语**：固定字段顺序 + 浮点 `R` 格式 + null 安全的**写**原语 ＋ 最小**解析**原语 `ParseValue` / `ParseObject` / `ParseArray` / `ParseString` / `ParseNumber`；DTO 字段布局仍留业务侧 —— 它是 `Json`（`MiniJson` 动态对象树）的**下层**，⛔ 不是它的替代品）、`ServiceAutoWire`（**反射装配 / 服务定位**：按接口契约在指定程序集找实现并实例化，候选按 `FullName` 序确定、两级类型缓存、多实现 / 找不到均可诊断；⛔ 不缓存实例、装配失败不抛）、`Setting`（本地存档）、`DeviceId`、`EntityPool.cs`（`EntityInfo` / `IEntityManager` / `IObjectPool` 契约）
- 日志横切：`LogThrottle`（降频闸门：**时间口径** `ShouldLog` / `*Throttled` / `*Once` + **计数口径** `ShouldLogEvery` / `*Counted`，可注入 `Clock`）、`LogBuffer`（运行时日志环形缓冲：最近 N 行只读窗口，补 `ILogger` 只写不读的缺口）
- `Game` 门面与 `EngineRunner`（自持隐藏 `MonoBehaviour` 宿主）
- `AssemblyInfo.cs`（`internal` 可见性声明，供测试程序集访问——Core 未开放给 Editor）

**禁止放入**

- 依赖其他任何引擎程序集（`Core` 的 asmdef 引用必须为空）
- 具体业务玩法逻辑、网络收发实现、资源加载实现、场景与 UI 表现

**判定标准**

一个类型**必须**放 `Core` 的唯一理由：**它会被 `Core` 的接口签名 / 事件 / 属性直接暴露**。
只是「比较底层」「暂时没地方放」都不构成理由。

**评审口令**

`Core` 只能是「契约 + 底座」，不能长成第二个 `Network` 或第二个 `Presentation`。

### 3.2 Data（数据域）

**允许放入**

- `DataTable`（读打表工具生成的强类型表）、`Localization`（多语言文本 + 语言切换事件）、`CloverData`（域内装配）
- `CloverTable`（**读自家打表工具产物的加载器**：目录解析 / 逐 tsv 读取 / 可定位错误串 / 按主键强类型取行）
- `FileSlotStore`（**一槽一文件**的存储设施：原子写 + 损坏留档 + 枚举）

**禁止放入**

- 打表**生成器**（在 [`clover-tools/table`](https://github.com/qw576483/clover-tools/blob/main/table/README.md)，不在引擎内）、生成物的手改
- 服务器下行数据的本地缓存 —— 它属 `Network` 的 `WorldSync` + `SchemaRegistryManager`，**不属于 `Data/`**
- 本地存档 `Setting` —— 实现落在 `Runtime/Core/Setting.cs`（能力上属数据域，**程序集归属 Core**，因为门面与契约在 Core）
- ⚠️ 与之互补的 `FileSlotStore` 是**纯文件设施**（不依赖任何 `Core` 契约，只用 `System.IO` + 引擎自带 `MiniJson`）⇒ 它**就在 `Data/`**。
  分工记牢：**「单文件 KV」放 `Core`（`Setting`）、「一槽一文件」放 `Data`（`FileSlotStore`）** —— 别把后者挪进 `Core`，也别把前者挪出来

**评审口令**

`Data/` 只放「策划与文案」，不放「与服务器同步的运行时数据」。

### 3.3 Network（网络域）

**允许放入**

- 连接与传输：`Connection`、`Transport`、`WebSocketConnection`、`CloverNet`
- 会话与协议：`Session`、`SessionCrypto`（会话通道加密：AES-256-GCM 整帧加解密 + 从回包提取密钥）、`Protocol`、`EMsg`、`ErrCode`、`Router`
- 高层能力：`NetworkManager`（路由 / Call 配对 / 重连 / UDP 绑定拦截）、`CloverAuth`、`CloverScene`、`FrameRoomManager`、`WorldSync`、`SchemaRegistryManager`、`AlertManager`、`NetworkSimulator`、`WebRequest`
- `LanBrowser`（局域网寻服：UDP 旁路查询，只做发现，不做连接；`Runtime/Network/Lan/` 子目录，见 §五 N13）
- `LanResponder`（`ILanResponder`，经 `CloverLan.CreateResponder()` 取得：应答「我开的主机要被别人扫到」—— 收查询 → **单播回**一条含主机信息的应答；固定监听 `47777`，**不经 `Game` 门面**（"我这台是不是主机"是业务决策）；见 §五 N13）

**禁止放入**

- 业务玩法规则、UI 表现、场景 / 实体 / 对象池等表现逻辑（进 `Presentation`）
- 权威数值计算（客户端不跑权威逻辑）

**判定标准**

核心问题是「消息从哪来、往哪去、怎么配对、断线怎么办」→ `Network`；
核心问题是「怎么显示」→ `Presentation`。

**评审口令**

`Network` 只负责「把消息送到并配对」，不负责「决定发什么」。

### 3.4 Resource（资源域）

**允许放入**

- 后端抽象：`ResourceBackend`（`IResourceBackend`）、`ResourceManager`、`CloverRes`
- 具体后端：`AssetBundleBackend`（以及后续按同一接口接入的 Addressables 等）
- 热更链路：`ResourceUpdater`（版本 / 清单）、`ResourceDownloader`（断点续传、边下边玩；当前**直用 `UnityWebRequest`**，未经 `Game.Http` 统一入口）

**禁止放入**

- 业务侧的「具体加载清单 / 加载时机」——那属业务
- 把原生 API（`Resources` / `AssetBundle` / Addressables）直接暴露给业务

**硬规则**

- 战斗内**禁止**同步加载资源、禁止裸 `Instantiate`。
- 新增资源后端**必须**经 `IResourceBackend` 落位，**不准**在业务侧另起一套加载。

**评审口令**

看到业务代码里出现 `Resources.Load` / `AssetBundle` 原生调用，直接判错。

### 3.5 Presentation（表现域）

**允许放入**

- 场景与实体：`Scene`、`CloverPresentation`、`Entity`、`ObjectPool`、`Map`（逻辑地图：服务端权威地图在客户端的**只读投影**，只回答"这一格能不能走"这类空间事实；含命名标记点查询 `Points` / `GetPoints(name)` / `TryGetPoint(name, out pos)`）
- 界面：`UI`、`UIWidgets`（Toast / 飘字 / Loading / 红点 / 确认框 / 引导遮罩 / `WorldHpBar`）、`UIFactory`（通用控件工厂：`CreateSlider` / `CreateInputField` / `CreateSelector` / `CreateToggleRow` / `SetBarWidth` ＋ 布局助手 `Place` / `AnchoredTopLeft` / `AnchoredBottom` / `CreateLabel` / `CreateBoxRect` / `CreateBottomLabel`；配色 / 文案 / 字号全部参数化）、`TextHooks`（通用件文字接管点：引擎建的每个 `Text` 都通知业务挂钩，像素风项目靠它把引擎提示也换成自己的字模）
- 实体视图来源（**可插拔接缝**）：`IEntityViewSource` + `EntityViewFactory.RegisterSource`（视图规格 → 视图内容的建造归业务；⛔ 不是"另起一套实体视图生命周期"）、`SpriteEntityViewSource`（2D 精灵实体视图：`SpriteRenderer` 节点 + 异步贴图 + 逐帧动画 + 纵深排序 + 池化回收；第 3 方实现同接口即可）
- 资源表现：`SpriteAtlas`
- 瓦片：`TileWorld`（2D 瓦片世界的**空间事实面**：实心格位图 / 托台小数顶高 / 世界边界；⛔ 不含位移解算）、`TileNodePool` / `TileRenderState`（`SpriteRenderer` 瓦片节点池 + 一格渲染状态值）、`TileRenderer`（**等距瓦片渲染内核**：一格一层的对齐 / 缩放 / 排序纯函数 + 无条件写全 + 计划落地；节点来源与取图 / 占位配色由调用方注入，⛔ 不持有池、不查可走性）
- 多媒体与交互：`Animation`、`Sound`、`Input`、`CloverInput`、`Camera`、`Quality`、`CloverFirstPersonCamera`（第一人称 rig：鼠标位移 → yaw/pitch、眼位 → 相机位姿、FOV 下发，并留后坐力 / 摇晃 / 视点晃动三个注入缝）、`ViewBob`（视点晃动 + 落地沉降，纯逻辑类 + `ViewBobConfig`）、`CameraMath`（FOV 换算 / 视线方向 / 指数跟随三条纯函数）、`LookAccumulator`（鼠标位移 → yaw/pitch 累加）—— 后四个是**相机侧纯件**，不经 `Game.Camera` 门面、业务自持

**禁止放入**

- 网络收发与协议解析（进 `Network`）
- 权威数值逻辑（客户端不跑权威逻辑）
- **不在引擎范围**：Spine / DragonBones 等骨骼动画 SDK、技能时间轴 / 帧事件表 —— 由业务接入或自研

**硬规则**

- **业务禁止直调 Unity `SceneManager`**，场景切换一律走 `Scene` 模块。
- UI **只订阅数据事件刷新**，不直连网络、不改数据。
- 同层模块之间不互相引用程序集（UI 不依赖 Scene，Sound 不依赖 Animation）。

**评审口令**

`Presentation` 只订阅数据、只做展示；出现网络调用或数值裁决就是越界。

### 3.6 Editor（Editor 横切）

**允许放入**

- `Debugger`（统计面板 / 网络区块 / 资源区块 / GM 控制台 / 网络模拟）
- `MapBake`（地图烘焙：读 Unity 关卡 → CloverMap 二进制；面板 + 菜单 + `-executeMethod` 命令行入口。`MapBakeOptions.MarkerRootName` = 导「命名标记点」段（留空 ⇒ 不导、不置 `FlagMarkers` 位）；`LayerFilterEnabled` / `LayerMin` / `LayerMax` / `LayerMinY` / `LayerMaxY` = 多层地图的**层过滤**（默认关，关着时与旧版逐字节一致））
- `EditorStartScene`（打开编辑器时打开启动场景：启动场景取 `EditorBuildSettings` 首条 enabled，**引擎侧不写死任何场景路径**；含 `Clover/编辑器启动场景/` 菜单与按工程隔离的开关）

**禁止放入**

- 任何运行时代码（玩家包拿不到 `Editor` 程序集）
- 把**某一款游戏的**烘焙参数（场景路径、地图名、出生点布局）写成引擎默认值 —— 那些属业务，经 `MapBakeOptions` 传入

**硬规则**

- Editor 程序集**只能**在 Editor 与 Development Build 下编译进包。
- 客户端 Editor 程序集**只有 Debugger、MapBake 与 EditorStartScene 三个横切**；配表代码生成由 [`clover-tools/table`](https://github.com/qw576483/clover-tools/blob/main/table/README.md) 负责，**不在引擎内新增生成器**。

### 3.7 Tests

**允许放入**

- `Tests/Editor`（EditMode）与 `Tests/PlayMode`（PlayMode）用例，以及 PlayMode 的测试脚手架（如 `MiniWsServer`）

**硬规则**

- 既有测试套件必须**能编译、能跑过**；改引擎代码把它们跑红了属于**必须修的回归**。
- 测试脚手架**不准**依赖 Unity/Mono 下不成立的 BCL 行为（例如 `HttpListener` 的 WebSocket 升级）。
- 跑测试的方法（承载工程 + `testables` + 两个 PlayMode 陷阱）见 skill `reference/unity-cli.md` §4.1。

---

## 四、编码规则

### 4.1 模块设计规则

1. **一程序集一主线。** 一个能力域必须有单一职责，不能同时承担多种明显不同语义。
2. **优先做文件 / 子模块，不优先做顶层目录。**
3. **薄模块要谨慎。** 只有 1~2 个文件、且没有清晰扩展方向的目录，优先合并。
4. **空目录不保留。** 不做「未来可能要用」的占位目录（`Runtime/Plugins/{Android,iOS,x86_64}` 是有明确落点用途的例外）。
5. **孤儿代码要清理。** 长期无人引用的实验实现，要么接入主干，要么删除。
6. **目录一旦建立，就必须有清晰边界。** 不允许靠约定俗成猜职责。

### 4.2 命名规则

1. 目录名必须表达**职责**，不表达历史来源。
2. 禁止同名不同义、同义不同名长期并存。
3. 同一种能力只允许一个稳定命名；不允许为了局部方便额外起别名目录。
4. **明确不做代码生成、必须手工维护的多处一致项**：`EMsg` 消息号、`Protocol` DTO、动画参数常量、多语言 key。
   两端 / 多处必须同时改；**不提供 Generator，也不提供自动生号**。
5. 配表代码（`DataTable` / `Localization`）**由打表工具生成，禁止手改**。

### 4.3 依赖方向规则

1. 模块程序集**只引用** `Core`；**禁止**模块之间互相引用。
2. `Core` 的 asmdef 引用列表必须为**空**。
3. 需要跨模块通信时，**经 `Core` 的契约 + `Event` 总线**，不要直接硬连具体模块。
4. 不允许为了图快跨程序集引用一个实现，把 §2.1 的分层打穿。
5. **禁止手改 `.asmdef` 的引用**来绕过编译期约束 —— 编译期挡不住的结构，运行期一定会烂。

**评审口令**

看到 `Core` 里出现 `CloverEngine.Network` / `CloverEngine.Presentation` 之类的引用，直接判错，不用讨论。
看到业务代码 `using CloverEngine.Presentation;` 却直接操作 `SceneManager`，直接判错。

### 4.4 复用规则

1. 发现已有同类能力时，先收敛到原能力，**不准平行再起一套**。
2. 发现同类实现多套并存时，必须明确谁是唯一真相包。
3. 新功能落地前，先搜索引擎里是否已有同类能力。
4. 已有能力不够用时，**优先扩展既有实现**，而不是新建并行实现。
5. **位图字模的度量 / 换行 / UV 切格 / bestFit / 字形回退**：唯一真相 = `Runtime/Presentation/BitmapFont.cs`。
   业务侧再写一套"按步进累加量宽 / 贪心断行 / 格位算 UV" ⇒ 直接判错，改为实现 `IGlyphSource` 把字模喂进去
   （素材 / 路径 / 简繁表仍留业务侧；⛔ 别把素材加载也搬进引擎）。

**必查能力清单**（写新代码前先确认引擎里有没有现成的）：

`Event` · `Timer` · `Fsm` · `Dispatcher` · `Logger` ·
`LogThrottle`（日志降频闸门，**时间 + 计数两种口径**：`ShouldLog`/`*Throttled`/`*Once` 与 `ShouldLogEvery`/`*Counted`；`Reset()` 一并清空） ·
`LogBuffer`（运行时日志环形缓冲：最近 N 行只读窗口 + `Version` + 线程安全入队；补 `ILogger` 只写不读的缺口） ·
`Json`（动态对象树：`MiniJson.Parse` / `Dump`，键序按 Ordinal 重排） ·
`JsonWriter`（**确定性 JSON 写 / 解析原语**：字段顺序 = 调用顺序、浮点 `R`、null 安全；要把 DTO 逐字段写成**逐字节固定**的文本时用它，⛔ 别在业务侧再手写一套 `WriteString` / 转义 / 数字解析） ·
`ServiceAutoWire`（**按接口反射装配实现**：组合根别每来一个模块就改一次装配；候选缓存 / 多实现诊断 / 降级尝试都在这里，⛔ 别在业务侧再写一套 `GetTypes()` + `Activator.CreateInstance` 扫描） ·
`Setting` · `DeviceId` ·
`EntityPool.cs`（`IEntityManager` / `IObjectPool` 契约） · `ObjectPool`（GameObject 池） ·
`TileNodePool`（`SpriteRenderer` 瓦片节点池：`Take` / `Return` / `Clear` + 纯函数 `SplitDemand`；池根由调用方给、取出即 `SetActive(true)`） ·
`SpriteEntityView`（**2D 精灵实体视图来源**：经 `EntityViewFactory` 的 `IEntityViewSource` 接缝接管实体视图 ——
`SpriteRenderer` 节点 + 异步贴图（请求序号守卫）+ 逐帧动画（`SpriteFrameAnimator`）+ 纵深排序（`SortingLayers`）+ 池化回收（`IObjectPool`）；
⛔ 写"2D 实体视图"前先查这里：别在业务侧再搭一套建/绑/销） ·
`TileRenderState`（**一格瓦片渲染状态值**：5 个 readonly 字段 + `SameAs`；配合"无条件写全"保池化前后逐项相等） ·
`TileRenderer`（**等距瓦片渲染内核**：`TileCellPlan` / `TileLayer` / `TileLayerParams` + 纯函数 `StateOf` / `LocalScaleFor` / `ColorFor` / `PlaceOfPx` + 无条件写全 `Apply` + `Build` / `ApplyPlan`；节点来源与取图 / 占位配色由调用方注入，⛔ 别在业务侧再写一套"格中心 + 像素对齐 + 排序号"） · `Router` · `Call` 配对 · `WebRequest` ·
`ResourceManager`（后端抽象） · `SpriteAtlas` · `UI` + `UIWidgets`（通用件：Toast / 飘字 / Loading / 红点 / 确认框 / 引导遮罩 / `WorldHpBar`） ·
`UIPanelGuards`（**面板参数取值守卫**：`TryGet<T>` 纯探测 / `Require<T>` + `RequireValue<T>` 探测 + 降级 + 只报一次；⛔ 别在业务侧再写一套 `Require<T>` 样板 —— 业务只留一层 per-tag 取值转发） ·
`UIFactory`（通用控件工厂：`CreateSlider` / `CreateInputField` / `CreateSelector` / `CreateToggleRow` / `SetBarWidth` ＋ 布局助手 `Place` / `AnchoredTopLeft` / `AnchoredBottom` / `CreateLabel` / `CreateBoxRect` / `CreateBottomLabel`；⛔ 写新控件前先查这里，配色 / 文案 / 字号一律参数化，别在业务侧再起一套） ·
`SpriteStripLoader`（**多帧条带 → 定长帧表**：整条 `LoadAll` 主路 + 逐帧按名兜底 + 同路径去重 + 就绪回调（**可能是同步的**）+ 纯函数 `Slice`；缺帧不静默；⛔ 不 Destroy 取到的对象） ·
`SpriteSwapButton`（**sprite-swap 按钮工厂**：常态 / 悬停 / 按下 / 禁用四态贴图 + 一张横排条带切 N 态；骨架走 `UIFactory.CreateButton`、取帧走 `SpriteStripLoader`；⛔ 配色 / 尺寸 / 文案 / 路径全部参数化；常态缺图 ⇒ 保留占位 + 只报一次，⛔ 不把 `null` 填进 `SpriteState`） ·
`NetworkSimulator` ·
`Sound`（音效播放闸门：**缺失只报一次** —— 四处 `clip == null`（`PlayBGM` / `PlaySFX` / `PlaySFXAt` / `PlayVoice`）整进程只报一次/路径 + 单帧起播上限 `MaxPlaysPerFrame` + 同路径**同时播放**上限 `MaxConcurrentPerClip`，两者默认 `0` = 不限；本文件内**已无裸 `Warn`**（池满告警也走 `LogThrottle`，池逻辑原样））
`ChunkedTilePlanner`（**大地图分块规划**：可见块范围 / 每帧节点预算 / 双缓冲换帧；纯逻辑，⛔ 不碰渲染、不建节点，节点池与取图仍由业务注入） ·
`TilemapGenUtil`（**程序化瓦片**：块级随机 DFS 生成树 + 环路、按四边开口/镜像拼 tileset + Stamp 叠加；块库与规则表全部由调用方以数据 + 委托传入） ·
`LoadingPacing`（**读条分档节奏**：进度 → 档、档 → completeness、节奏放行；⛔ 帧数与档位取值属业务） ·
`CameraBoundsKit`（**格空间相机夹制**：可见格矩形 ⊆ 地图 + 焦点安全边距；⛔ 与 `Camera.cs` 的世界 AABB 钳位**口径不同**，等距工程用这个） ·
`SceneScaffold`（**Editor 横切**：最小可运行场景（正交相机 + 可选 URP `Light2D` + 命名根节点 + 入口脚本）+ 幂等写 Build Settings + 设 Play 起始场景 + 反射解析业务类型 + 启动自愈体检；⛔ 场景名/相机参数/根节点名/入口类型全部参数化，不许写成引擎默认） ·
—— ⛔ 写"音效发放"前先查这里：⛔ 别在业务侧再搭一套探测缓存 / 帧计数 / 并发表，也别裸 `Warn` 报缺失

---

## 五、契约硬约束

> 本节两条表（N / G）是**长期有效的对外契约**，不是实施计划。列在这里的编号可在注释与提交中被直接引用。

### 5.1 网络与会话（与服务端的契约）

| # | 约束 |
|---|---|
| N1 | 单一 `addr = ip:port` 配置；启动自动连接，无手动输入 |
| N2 | 帧格式 `[4B requestID][4B msgID][body]`，requestID==0 推送；TCP 外加 `[1B type][4B length]`、QUIC 流外加 `[4B length]`（**无 type**）防粘包；裸 UDP 首字节 `0x55`；**单帧上限三条线路统一 10 MiB**（超限本地 `ClientFrame.Encode` 抛 `ArgumentException`，线路层软超告警、硬超断线） |
| N3 | EMsg 消息号**手工维护**：引擎段 `[1,10000]` 由引擎定义，业务段 `>= 10001` 由业务 def 包编排；**不提供代码生成、不提供自动生号**，两端同名同值需同时改 |
| N4 | 重连走 session resume 不重登；次数上限 + 指数退避，超限抛事件停手 |
| N5 | UDP 绑定两步：服务端经 TCP 下发 `EMsg.UDPBindGrant`（体为令牌原文）→ 客户端经 UDP 上报 `EMsg.BindUDP`（体为令牌原文）；每 10 秒重报绑定帧（兼 NAT 保活）、断线清理；UDP 推送与可靠推送进同一条 Router，业务不感知线路 |
| N6 | QUIC、WebSocket、RawUDP 为 **P0 级交付**（**WebTransport 未实现**，见本文件 §五 N10 的家族划分），不允许列为“可选插件”；TCP 与 RawUDP 为 P0 基础连接；WebSocket / QUIC 在对应平台启用后必须完成适配、测试和线路选择，才算客户端网络层完成 |
| N7 | **全场景禁止自签证书**；引擎不提供跳过证书校验的开关 |
| N8 | WebTransport keepalive（msgID==0 空包）直接丢弃，不进 Router |
| N9 | 多设备被踢 → 清理 UDP → 走 `OnKicked`，不自动重连 |
| N10 | **平台线路按家族划分，两家族互斥**。原生家族（Standalone / 移动端）= `QUIC → TCP + RawUDP`；Web 家族（WebGL）= `WebTransport → WebSocket`。**注意 Web 家族这两条线路当前都未落地 —— 两者都需浏览器侧 jslib 桥接，因此 WebGL 上「应使用的线路集合」如实为空**（`Runtime/Network/Transport.cs:204-223`：`IsSupported` 在 WebGL 恒 false；`:267-274`：`PlatformSummary` 明写"当前无可用线路"），**不做"声明可用、连接必失败"的假可用**；WebSocket 在原生端虽能跑，但属 Web 家族、不参与原生线路规划。主链与降级链按平台配置，**降级只在家族内发生**；跨家族换线**不是降级**，它会静默换掉通道语义（WS 没有真正的不可靠上行，`SendUnreliable` 会退化成可靠发送并把队头阻塞吃满），因此被禁止。所有线路必须满足同版本对齐后才算完成 |
| N11 | 线路 TLS：`GameConfig.UseTls` 与服务端 `gateway.tcp_tls_disabled` **必须相反**；证书只走系统信任链（无跳过校验的开关） |
| N12 | 排队与通道加密：由引擎自动处理（`EMsg.QueuePosition` 位置帧 + 登录时自动协商 AES-256-GCM 通道加密）；业务**不得**手填 `ELoginRequest.encrypt`，也不得自行加解密帧 |
| N13 | 局域网寻服（`LanBrowser`）是**旁路协议**：不占 EMsg 消息号、不进 Router、不走线路族，与服务器端 etcd 服务发现无关。仅原生平台可用（WebGL 无 BSD socket ⇒ `IsSupported=false`，禁止静默失败）。使用顺序固定为「`Game.Launch` → `Game.LanBrowser.Scan` → 选主机 → `CloverNet.Init(主机 Address)`」；引擎不提供运行中切服（`CloverNet.Init` 幂等，见 N1）。应答端 `ILanResponder`（`CloverLan.CreateResponder()`，**不经 `Game` 门面**）同属该旁路：固定监听 UDP `47777`（与发现端临时端口不同）、**只单播回查询来源**（⛔ 不广播回包）、收发复用 `LanSocket` / 编解码复用 `LanProtocol`（⛔ 不写第二套 UDP 收发）；`Start` / `Stop` 幂等，四条边界（平台不支持 / 端口占用 / 重复 `Start` / 未启动 `Stop`）都**不抛**、原因走 `LastError` |

> 编号只增不改、不复用空号（改语义 = 断掉所有引用它的注释与实现）。

### 5.2 通用硬约束

| # | 约束 |
|---|---|
| G1 | 模块对外只暴露接口 / 纯数据 / 枚举 / 工厂；实现类 `internal`；工厂返回接口 |
| G2 | 所有业务回调（OnMsg / Timer / Event / 动画事件）必须在主线程；公开 API 仅主线程可调（**引擎未内置主线程断言**，靠约定与评审保障） |
| G3 | Entity 不继承 `MonoBehaviour`；View 异步绑定 |
| G4 | 客户端不跑权威数值逻辑；重连后以服务器快照覆盖本地 |
| G5 | 战斗内 `GameObject` 一律走对象池；资源一律走 `Resource` 模块异步加载 |
| G6 | 场景切换一律走 `Scene` 模块；业务禁止直调 Unity `SceneManager` |
| G7 | 配表由打表工具生成；**消息号、动画参数常量、多语言 key 手工维护**（两端 / 多处必须一致） |
| G8 | 引用类型**跨程序集**传递不得让渡可变状态：或返回**拷贝快照**，或返回**只读契约 / 不可变对象**（判据与复核结论见 5.3） |
| G9 | 返回接口的方法判 `null` 后再返回，避免 typed-null 等价坑 |
| G10 | 函数注释不写冗余函数名 |
| G11 | UI 只订阅数据事件，不直连网络、不改数据 |
| G12 | 冻结画面的定时器：`Time.timeScale = 0` 期间要触发的延时必须用 `Timer.AfterUnscaled` / `EveryUnscaled`（普通 `After` / `Every` 不触发） |
| G13 | 资源一律经 `Game.Res`：不许用 `Resources.Load` / `AssetBundle.*` 绕过资源模块；需要"立刻拿到"就先 `Preload`、后 `TryGet<T>` |

### 5.3 G8「跨边界」判据（2026-09-15 复核并落定）

**判据**：G8 的"跨边界"= **跨 asmdef 程序集**（同程序集内部传引用是正常 C# 用法，不受约束）。
其实质要求是 —— **不让外部通过拿到的引用直接修改模块内部的可变状态、或绕过契约方法改状态**。
满足方式两条**等价**，按读取频率选：

| 方式 | 适用 | 代价 |
|---|---|---|
| ① 返回拷贝（快照） | 低频、数据量小 | 每次调用分配 |
| ② 返回**只读契约 / 不可变对象** | **高频读取**（实体、快照数据） | 无分配，但需按只读设计 |

**四处字面存疑点的复核结论**：

| 位置 | 结论 | 处理 |
|---|---|---|
| `IEntityManager.Create / Get / GetAll / GetByGroup` 返回内部实例 | **违反**（外部可绕过 `BindView` 直接改状态） | 采用方式 ②：`EntityInfo` 改为**只读快照**（只读属性 + Core 工厂），`View` 移出并改由 `IEntityManager.GetView` 读 |
| `EntityInfo.Attrs`（可变 `Dictionary<string, object>`） | **违反**，且全仓 **0 处引用**（已被 `ObjectData` 的强类型容器取代） | 直接删除该字段 |
| `CloverScene` 把 `this` 作为事件载荷 `Emit` | **不构成越界** | 实现类 `CloverSceneManager` 是 `internal`，跨程序集只能按 `ICloverScene` 使用 ⇒ 拿不到额外可变状态；代码处已写明豁免理由 |
| G9（返回接口先判 null） | 未发现违反 | — |

---

## 六、文档与注释规则

1. 模块职责 / 依赖方向变化后，本文件与 `clover-client-unity-engine-index.md` **必须同步更新**。
2. 代码注释里的契约引用必须**指向真实存在的编号与文件**（如 `见 结构规则.md §五 N10`），不准留悬空引用。
3. 不允许出现「代码改了，但注释 / 索引文档还是旧世界」的情况。
4. `Runtime/**` **不逐模块放 README**，实现注释即文档；跨模块的说明写进本文或索引文档。
5. `~` 结尾的目录（`Samples~` / `Tools~`）Unity 不扫描、也不为它们生成 `.meta`/构建资产，因此**不放运行时代码**；
   唯一例外是 UPM 样例 `Samples~/LoginFlow/`（由 `package.json` 的 `samples` 字段声明，用户导入后即成为其工程内的运行时代码，自带 `.asmdef`）。

**评审口令**

结构改了而文档 / 注释没改，视为这次结构改动**未完成**。

---

## 七、agent 执行约束

以下行为一律视为**未完成**：

1. 只建目录 / 只加 asmdef，不迁真实文件
2. 只改 `using`，不删既有实现
3. 只迁代码，不修注释与索引文档
4. 保留或新增**模块之间的 asmdef 引用**（破坏 §2.1）
5. 在 `Core` 里引用其他引擎程序集
6. 写「后续考虑」「视情况处理」这种模糊结论
7. 把临时落点写成长期正式结构
8. **不做验证就宣称完成** —— 客户端结构类改动至少必须跑：
   - 按 asmdef 边界分步编译（Core / Data / Network / Resource / Presentation 各自可编译）；
     `Runtime/Core` 改动可直接用 `Tools~/core-assert/` 离线宿主验证（不起 Unity）
   - 动了 `Tests/` 或运行时行为时：`unity test <承载工程> --mode EditMode|PlayMode`

**交付最低要求**：任何结构类任务交付时必须说明改了哪些目录 / 删了哪些旧目录 /
还有没有旧的 `using` 残留 / 上述三项验证是否通过 / 注释与索引文档是否同步。缺任意一项都不算完整交付。

---

## 八、代码评审提醒

看到下面这些情况，应直接视为**结构告警**：

1. 模块之间出现 asmdef 互相引用（含 `Core` 引用其他域）
2. 被 `Core` 契约暴露的类型定义在非 `Core` 程序集
3. 新增一级能力域（第 6 个 asmdef）却说不清归属
4. 在 `Network` 里写业务玩法决策
5. 在 `Presentation` 里直接收发网络消息 / 做数值裁决
6. 在 `Data/` 里塞服务器同步的运行时数据
7. 业务代码直调 Unity `SceneManager` / `Resources.Load` / 裸 `Instantiate`
8. 新增同类并行实现而不是扩展现有能力
9. 创建空目录、占位目录、孤儿实验代码
10. 同义不同名、同名不同义的脏命名长期共存
11. 结构改了但注释 / 索引文档没跟着改
12. 用「先这样放着」当正式方案
13. 往 `Samples~` / `Tools~` 放运行时代码

### 结构类改动必查清单

- [ ] 新增目录 / 模块是否属于 5 个能力域之一
- [ ] 是否复用了已有能力，而不是新起一套
- [ ] 是否引入了模块间 asmdef 引用，或让 `Core` 依赖了别人
- [ ] 被 `Core` 契约暴露的类型是否确实定义在 `Core`
- [ ] 是否把业务决策错误塞进了 `Network`
- [ ] 是否把网络 / 数值逻辑错误塞进了 `Presentation`
- [ ] 是否把脏路径留在注释 / 索引文档里
- [ ] 是否留下空目录或空壳目录
- [ ] 按 asmdef 边界的分步编译是否通过
- [ ] 是否说明了完成标准

---

## 九、一句话执行口令

以后做任何新代码、新目录、新程序集时，先问自己：

> **它到底属于 Core、Data、Network、Resource、Presentation 的哪一类？**

如果答不清，先别写。
